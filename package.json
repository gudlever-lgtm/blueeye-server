{
  "name": "blueeye-server",
  "version": "0.1.0",
  "private": true,
  "description": "BlueEye server — on-prem, single-tenant API (Node/Express + MySQL).",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "test": "node --test"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.11.5"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
