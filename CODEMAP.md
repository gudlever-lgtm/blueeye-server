# BlueEyes server — Code map

On-prem network‑monitoring + central‑licensing server. **Node.js + Express + MySQL**,
CommonJS (`require`/`module.exports`), **no build step**, dependency‑free vanilla‑JS
dashboard. This file is a navigation aid: what lives where, and where to change things.

> Sister repos: **blueeye-agent** (runs on customer machines, reports traffic/probes)
> and **blueeye-licens** (signs license proofs verified here).

## Boot flow

```
src/server.js      start(): builds db + repositories + services, wires createApp, listen(),
                   starts the retention scheduler + WS servers, graceful shutdown.
  └─ src/app.js    createApp({ ...deps }): Express app (no listen) — static dashboard,
                   json body, request logger, mounts the API router, 404 + error handler.
       └─ src/routes/index.js  createApiRouter({ ...deps }): mounts every feature router.
```

Everything is **dependency‑injected**: `createApp`/`createApiRouter`/`createXRouter`/
`createXRepository`/`createXService` take their deps as arguments, so the test suite
swaps in fakes (`test-support/fakes.js`). `src/server.js` is the only place that wires
the *real* MySQL pool + concrete services.

- `src/config.js` — env‑driven config (`.env`), production safety checks.
- `src/db.js` — mysql2 pool + `ping`/`close`. `src/migrate.js` — numbered migration runner (`npm run migrate`).
- `src/logger.js` — leveled logger. `package.json` scripts: `start`, `dev`, `migrate`, `test`.

## Directory map

```
src/
├── server.js, app.js, config.js, db.js, migrate.js, logger.js   # boot + infra
├── routes/            # one router per resource (HTTP API) — see table below
│   ├── agents/        # /agents split by JOB: crud.js (the record) · commands.js
│   │                  # (what the server PUSHES to a host) · releases.js (signed
│   │                  # artefacts) · flows.js (pure aggregator) · _context.js
│   └── nis2/          # /api/nis2 split by RESOURCE: risks · controls · incidents
│                      # · evidence · reports · generator · exports · audit · meta
├── repositories/      # data access (one per table-ish); pool in, plain objects out
├── auth/              # user JWT (roles) + separate agent-token auth
│   ├── jwt.js password.js roles.js middleware.js   # user side (requireAuth/requireRole)
│   ├── provision.js                                 # shared JIT user provisioning for external auth (LDAP/OIDC/SAML)
│   ├── ldap.js                                      # external LDAP/AD auth (supplements local login)
│   ├── oidc.js saml.js samlXml.js                   # SSO: OIDC (auth-code+PKCE) + SAML 2.0 (SP-initiated, hand-rolled XML-DSig), EU/self-hosted IdPs
│   ├── securityPolicy.js securityGate.js passwordHistory.js  # baseline security (mig 041): password history, max age, role IP allowlist — docs/security-hardening.md
│   ├── apiTokenAuth.js                              # API tokens (api_access): authenticates X-API-Key / Bearer → req.user
│   └── tokens.js agentAuth.js                       # agent opaque-token side
├── integrations/      # outbound API integrations (ITSM/IPAM connectors)
│   ├── dispatcher.js  # trigger layer: events → enabled targets (debounce/retry/audit)
│   ├── httpClient.js  # shared outbound HTTP (injected fetch + timeout)
│   └── connectors/    # serviceNow.js, nautobot.js, webhook.js, customItsm.js, customCmdb.js + index.js (registry) + presets.js
├── diagnostics/       # Test area — outbound connectivity + security screening
│   ├── screening.js   # pure, explainable security-posture checks (per target)
│   └── reach.js       # injected-fetch reachability probe (no SSRF block)
├── analysis/          # local, explainable anomaly detection (NO ML, NO cloud)
│   ├── baselines.js detector.js findings.js correlator.js pipeline.js ingest.js
│   ├── probeFindings.js probePipeline.js  # probe results → findings + alerts
│   ├── assistant.js  config.js constants.js dependency-graph.json types.js
│   ├── networkReport.js  # findings → the executive "fix these, here" document
│   ├── alerting/      # email/webhook/matrix/syslog channels + dispatcher (config.js)
│   └── retention/     # rollup + purge + nightly scheduler (config.js, repo.js)
├── serviceTests/      # SERVICE ASSURANCE — no-code synthetic monitoring of web apps.
│                      # "Know when your digital services stop working — before your
│                      # users do." Self-contained by design (docs/service-assurance.md
│                      # §2): nothing in here requires a BlueEye module; the host
│                      # supplies db/secrets/audit/logger through ports.js, and
│                      # index.js (createServiceTestsModule) is the ONLY thing it
│                      # constructs. NOTE the naming split: the PRODUCT is Service
│                      # Assurance (nav, header, licence label); the internal
│                      # identifiers stay service_test* (dir, tables, API mount, key).
│   ├── index.js ports.js          # the module factory + the adapter boundary
│   ├── engine/        # the neutral DSL — dsl.js (22 step types) · validate.js ·
│   │                  # targeting.js (role→label→text→…→css) · redact.js. PURE:
│   │                  # a stored test never mentions Playwright.
│   ├── runner/        # driver.js = THE ONLY file that requires playwright-core;
│   │                  # execute.js (pure dispatch onto a driver interface) ·
│   │                  # classify.js (failure → plain language) · artifacts.js ·
│   │                  # pageSnapshot.js (the read-only script run inside the page)
│   ├── discovery/     # crawl.js (scope + budgets) · extract.js (pure) ·
│   │                  # safety.js (fail-closed destructive-action classification)
│   ├── suggest/       # rules.js — six rule-based proposals, each with its reason. NO AI.
│   ├── scheduler/     # schedule.js (pure) · queue.js · worker.js (the claim loop)
│   ├── security/      # hostPolicy.js — the two-check SSRF decision (permanent
│   │                  # deny-list ∩ per-application allowlist incl. CIDR) +
│   │                  # allowlistIo.js (CSV import/export)
│   ├── settings/      # DB-backed limits: defaults.js (catalogue + bounds) + the
│   │                  # effective-settings service. No env var holds a limit.
│   ├── validation/    # HTTP input validators (swept by the validation gate)
│   ├── api/           # the routers mounted at /api/service-tests
│   ├── stats/         # period.js — what "this week"/"March"/"2026" mean, and
│   │                  # the buckets a period is drawn in (day→hours, year→months)
│   ├── assurance/     # the REACTION layer: TLS certificate watch, incident policy, the sweep
│   ├── monitors/      # the checks that are NOT a browser — types.js (the catalogue),
│   │                  # smtpClient.js/imapClient.js (written here: the phase timings
│   │                  # are the diagnosis), checks/ (mail, dns, rbl, ldap, ntp,
│   │                  # tlsPort, tcpPort, db), policy.js, registry.js
│   └── storage/       # repositories over the service_test_*/service_monitor* tables
│                      # (migrations 078-080, 094)
├── diagnose/          # SYMPTOM-FIRST DIAGNOSIS — "describe the problem" → a plan →
│                      # verdicts. Catalogue first, LLM as a helper; it works
│                      # fully with AI off.
│   ├── playbooks/*.json  # THE KNOWLEDGE, as data. One file per fault, every
│   │                     # human string {en,da}. A new playbook is a new file —
│   │                     # no code change. Validated at BOOT; a bad one stops
│   │                     # the server (docs/diagnose.md).
│   ├── expr.js        # the rule language: hand-written tokeniser + recursive-
│   │                  # descent parser, NO eval/Function. Three-valued — a
│   │                  # missing measurement is `unknown`, never false.
│   ├── catalog.js     # loads + validates playbooks (test types against
│   │                  # PROBE_TYPES, views against the real screens, every rule
│   │                  # path + fix placeholder against FACT_SCHEMA, en/da parity)
│   ├── facts.js       # FACT_SCHEMA + buildFacts(): probe results → the object
│   │                  # rules read. Absent ≠ zero. Owns the ICMP-rate-limiting
│   │                  # reading rule (sustainedLossFromHop)
│   ├── match.js       # keyword/symptom matching, en+da, Danish stemming +
│   │                  # inverse-frequency weighting. Pure, deterministic
│   ├── evaluate.js    # verdicts (confirmed/ruled_out/inconclusive + WHY) and
│   │                  # placeholder-filled fixes. Decided in code, never by AI
│   ├── plan.js        # matched playbooks → the plan + the deduplicated test list
│   └── llm.js         # the OPTIONAL AI step: maps free text onto catalogue ids.
│                      # Every failure returns null and the caller falls back and
│                      # says so. The description is data, never an instruction
├── troubleshooting/   # consolidated fault view — pure read-model (overview.js) +
│                      # fan-out (overviewService.js). Aggregation only, no tables.
├── geo/               # flow extraction + offline GeoIP/ASN enrichment + storage
│   ├── extractFlows.js enricher.js provider.js privateIp.js flowPipeline.js
│   ├── centroids.js countryCentroids.json
│   └── hopLocation.js hostnameHints.js networkPlaces.js cityProvider.js
│                      # traceroute hops on the map: router name → city GeoIP →
│                      # country, each checked against the hop's RTT
├── flows/             # traffic-type categories (DNS/Facebook…) — categories.js
├── enroll/            # frictionless enrollment: agentSourceStore.js (source bundle
│                      # + cached SHA-256, REPRODUCIBLE — the script carries the
│                      # checksum and the tarball is a later request, so identical
│                      # source must hash identically; needs GNU tar),
│                      # installScript.js (Docker/Node installer),
│                      # artifactStore.js (legacy binaries), fingerprint.js (cert pin)
├── license/           # Ed25519 license-proof verify + feature gate
│   ├── verify.js publicKey.js licenseManager.js licenseCache.js features.js
├── ws/                # WebSocket servers: agentSocket.js, dashboardSocket.js
├── services/          # settings.js, systemInfo.js, enrollmentStore.js
├── validation/        # per-resource input validation (pure, { value | errors })
├── lib/               # csv.js (RFC4180 + injection guard), canonicalize.js, secretBox.js (AES-256-GCM secrets at rest)
└── middleware/        # asyncHandler.js, errorHandler.js, requestLogger.js

public/                # dependency-free dashboard SPA
├── index.html         # shell + top tab bar (data-view buttons)
├── app.js             # the whole SPA: views.*, render(), el() DOM helper, api()
├── about.js           # About: the dated feature history (data, { en, da }) + its page
└── styles.css         # design tokens (radii/elevation/hairlines) + light default
                       # + [data-theme=…] palettes (dark, nord, …); see docs/design.md

migrations/NNN_*.sql   # numbered, tracked in schema_migrations; the source of truth
schema.sql             # full snapshot, GENERATED from migrations/ (npm run build-schema)
docs/                  # per-feature docs (analysis, geo, alerting, retention, diagnose, ...)
scripts/               # seed-superadmin.js, seed-demo.js, dev-bootstrap.js, deploy.sh, build-schema.js
test/, test-support/   # node --test specs + fakes (makeApp, makeXRepo, authHeader)
test/gate/             # pre-build gate: security / ui / validation sweeps (scripts/gate.sh, docs/gate.md)
```

## HTTP API (routers)

Mounted in `src/routes/index.js`. User endpoints use JWT + roles
(`viewer` < `operator` < `admin`); agent endpoints use the opaque agent token.

| Mount | File | Auth | Purpose |
| --- | --- | --- | --- |
| `/health` | health.js | none | liveness + db ping |
| `/auth` | auth.js | none | login → JWT (tries **LDAP/AD** first when enabled, else local); `/sso` lists live SSO methods for the login screen; **`/change-password`** (authenticated) — forced first-login change for one-time-password users + self-service change, revokes older tokens |
| `/auth/oidc` | oidc.js | none | **SSO (OIDC)** browser flow: `/login` (302 → IdP, PKCE) + `/callback` (verify id-token → JIT user → JWT). Gated by `sso_oidc` |
| `/auth/saml` | saml.js | none | **SSO (SAML)** SP-initiated flow: `/login` (302 → IdP) + `/callback` (ACS: verify signed assertion → JIT user → JWT) + `/metadata`. Gated by `sso_saml` |
| `/users` | users.js | admin | user CRUD (last-admin protected) + **local invite with a one-time password** (`POST /local`, `POST /:id/resend-temp-password`, `GET /local-availability`) — only when no SSO/LDAP is active (else 403); emails the password, forces a first-login change. See docs/user-local-otp.md |
| `/me` | me.js | viewer+ | current user: profile + **personal UI preferences** (colour theme) |
| `/locations` | locations.js | viewer+/op/admin | sites + per-location live traffic |
| `/agents` (3 routers) | agents/ (index · crud · commands · releases · flows) · agentReports.js · agentEnroll.js | JWT / agent-token / none | CRUD + run-test + **run-probe** + **install-tool** (operator+, install a missing diagnostic tool on the host — allowlisted, audited). The privileged pushes (upgrade/delete/install-tool) are **signed** with the release key (`services/commandSigner.js`) so the agent can verify the SERVER asked, not merely something holding its socket + **connection** (viewer+, explainable why-is-it-disconnected verdict) + **reconnect** (operator+, force a live agent to re-dial — docs/agent-connection.md); agent self-report (`/results`, `/probe-results`, `/me/config`, `/me/capabilities`); enroll |
| `/enrollment-codes` | enrollmentCodes.js | operator+ / admin | enrollment codes (single-use or **bulk / multi-use**); admin deletes: `DELETE /:id` and **`DELETE /expired`** (bulk cleanup of the codes that timed out unused — `{ deleted: n }`, never touches a `used` code; backs the Enrollment page's "Delete all expired" button) |
| `/enroll` | enroll.js | none | **frictionless enrollment**: `/config`, `/agent-source.tgz` (agent source bundle + SHA-256, served locally — air-gap-friendly; `?sha=` asks for one exact build and 409s when the server has repackaged since), `/agent-source.sha256` (the checksum it serves right now, so a script can say which side of a mismatch is stale), `/agent-release(.tgz)` (signed release + manifest), `/agent-release-key` (release public key the agent pins for signed self-updates), `/uninstall.sh`, `/repin.sh` (re-anchors an INSTALLED agent to the release key this server serves now — no enrollment code, so it can never create a second agent; 404 while no key is configured), `/agent/:platform` (legacy pre-built binary), `/:code/install.sh` (self-contained installer: verifies the source, then installs natively via Node+systemd by default — Docker opt-in via `BLUEEYE_RUNTIME=docker`), `/:code/install.ps1` + `/uninstall.ps1` + `/update.ps1` (Windows PowerShell: install, remove, and **update-in-place** — the updater carries no enrollment code and aborts unless an enrolled agent is already on the host, so it upgrades that agent instead of creating a new one; all three take `?download=1` to come back as a saveable attachment) |
| `/api/enroll` | enrollCommand.js | operator+ | **install-command generator** (`/command`: one-liner + manual/checksum; mints or reuses a code) + **update-command generator** (`/update-command`: the Windows update-in-place one-liner, no code in it — what the Agents "Update" button shows for a Windows agent that is behind) |
| `/license` | license.js | viewer+ | license status + features + plan/usage/**matrix** (feature `status`: available/roadmap) |
| `/api/api-tokens` | apiTokens.js | admin (gated `api_access`) | **API tokens** — mint/list/revoke; secret shown once, hashed at rest |
| `/api/audit-log` | auditLog.js | admin (gated `audit_log`) | **unified audit log** — auth/user/role/license/report/api_token events; `?category=&user=&limit=`, `/categories`, `/verify` (walks the hash chain; a break carries `brokenAt` + `reason: altered\|unlinked`). UI **Administration → Audit log integrity** (`public/views/auditLog.js`) |
| `/system` | system.js | viewer+ / admin | storage/disk + **MySQL & TimescaleDB** db sizes (split) + ingest estimate; `GET /version` (this server + the agent it serves + **`upstream`**: the newest published versions, learned from the signed licence proof — no extra outbound call, see docs/updates.md); `POST /agent-release/publish`, `POST /agent-source/reload` (admin); **`POST|GET /server-update`** (admin — run/follow the host's configured deploy script, opt-in via `SERVER_UPDATE_COMMAND`) |
| `/api/findings` | findings.js | viewer+ | analysis findings (list filterable by `hostId`/`severity`/`metric`/`since`) + **`GET /summary`** (aggregate overview — counts + avg/max deviation grouped by severity/metric/host, `FindingStore.summary()`, backs the Analysis Overview panel) + **`GET /trend`** (the same filters bucketed by `hour`/`day` — counts per severity per bucket, the *when* behind the summary's *what*) + **`GET /report`** (the executive document: JSON, or `?format=html` for print→PDF, `?days=1..365`, `?locale=en|da`) + **`POST /ack`** (bulk acknowledge — explicit `ids` (≤1000) or an explicit `all:true` + the same filters; audited with the filter it ran) + ack + **`GET /:id/context`** ("what changed before this": change-type timeline events in a window before the finding's trigger, default 30 min — reuses the target-timeline merge) |
| `/api/assistant` | assistant.js | viewer+ (gated) | opt-in AI: `/explain` (per-host Q&A) + **`/location-summary`** (per-location "what's going on?") |
| `/api/geo` | geo.js | gated | geo overview + flow selection |
| `/api/alerting` | alerting.js | admin | channel config + test |
| `/api/severity-rules` | severityRules.js | viewer+ read / **admin** write | "this kind of event is a warning for us, not a critical" — CRUD over `event_severity_rules`, `POST /:id/apply-to-open` (back-fill open events; counts on a dry run, changes only on `{confirm:true}`), `POST /preview` (what a rule would do to one event, or with `{rule, scope:'open'}` — admin — how many OPEN events a draft would change: the editor's Preview button; stores nothing). Applied at STORE time in `FindingStore.save()` and the SA reactor, so alerting reads the ruled severity and history records what was decided at the time. Every touched event keeps `original_severity` + `severity_rule_id`. See `docs/severity-rules.md` |
| `/api/map` | map.js | viewer+ | effective tile/geocoder config |
| `/api/settings` | settings.js | admin | editable map / **analysis** / **retention** / **flow-categories** / **AI assistant** (enable + provider + API key + model + custom endpoint) / **alerting** (channels + secrets, write-only) |
| `/api/export` | export.js | viewer+ | CSV/JSON export + **investigation bundle** (`/investigation`: per-agent health+probes+interfaces+findings+flows, JSON or event-log CSV; print→PDF client-side) |
| `/api/flows` | flows.js | viewer+ | **traffic-type categories** (`/categories`) + **conversation explorer** (`/explore`: talkers/ports/protos/series + scan/fan-out) + **traffic map** (`/map`: one colored directional arc per site×country×category — agent/location/fleet scope, country centroids, in/out byte split; backs Flows→Map, the Overview map and the location page) |
| `/api/transactions` | transactions.js | viewer+ read / admin write | **transaction tests** (http/tcp/dns/icmp) — CRUD (secrets write-only, `{{secret:name}}` refs) + `PUT /:id/agents` + `GET /:id/results?from&to&agent_id` + `GET /:id/heatmap?from&to&bucket` (avg_latency/fail_count/sample_count per bucket per agent) + `GET /:id/trend?agent_id&days` (median per day per step) + **`POST /:id/run`** (operator+: run one test on one agent NOW, optionally keeping the packet headers; answers with the result AND its phase verdict) + **`GET /:id/captures`** / **`GET /:id/captures/one?agent_id&time`** (summaries; the packet list only on the second). Agents get config + report results over `/ws/agent` (`transaction_config`/`transaction_result`/**`transaction_capture`**); a crossed threshold is a FINDING raised through the finding sink (`src/devices/findingSink.js` — store, event case, alert, integrations; phase + errno + site-vs-system verdict in the explanation), not a direct dispatch. **Phases + capture: [docs/transaction-capture.md](docs/transaction-capture.md)** — `step_phases` splits each step into dns/tcp/tls/ttfb/transfer (`tcp` IS the network RTT; `think ≈ ttfb − tcp` is the server), verdict derived on read by `src/analysis/transactionPhases.js`; captures are header-only, filter derived from the test, kept only on fault, verdict stored once by `src/analysis/captureAnalysis.js`. Repo `src/repositories/transactionsRepository.js`, validation `src/validation/transactionValidation.js`, alert eval `src/analysis/transactionAlerts.js`, MAD baseline job `src/analysis/transactionBaselines.js` |
| `/api/probes` | probes.js | viewer+ | **active-probe** results (ping/tcp/dns/traceroute/**tcptraceroute**/**path_mtu**/**http**/**curl**/**pageload**/**transaction**); `/path?probeType=` → **path-visualisation graph** (hop nodes+links with loss/latency/jitter + GeoIP/ASN + ECMP `branches`, `src/analysis/pathGraph.js`; `probeType` picks `traceroute` (default) or `tcptraceroute` — the TCP-SYN trace that still gets through where ICMP/UDP is filtered. The two are never merged into one graph, and a TCP target is stored as `host:port`); `/path/metrics` → metric catalogue; `/path/timeseries` → bucketed metric series (per-agent `overlay`, DST-safe, `src/analysis/pathTimeseries.js`) for the shared Path Visualization timeline |
| `/api/connection-test` | connectionTest.js | viewer+ read · **operator+** run/schedule | **CONNECTION TEST** — one address, the whole battery of checks, from one agent (Probes & Tests → Connection test). `GET /checks?host=` serves the catalogue (`src/connectionTest/checks.js`) — what exists, what the agent can actually run (reverse DNS and the TLS certificate check are listed with `available:false` rather than hidden), and whether each applies to THIS target (a `dns` lookup of an IP literal does not). `POST /run` pushes every selected check to the agent as ordinary `run-probe` commands — 202 + what was dispatched and what was skipped, 409 when the agent is not connected — so results land in `probe_results` and every existing screen already understands them; one operator action is ONE hash-chained audit record, not nine. `POST /schedule` saves the same test as a recurring test package (`test_packages.schedule_spec`, mig 099) through the ordinary package validator, and it consumes an active-test-path slot like any other enabled package. The browser never builds a probe spec: the catalogue exists once, on the server. See `docs/connection-test.md` |
| `/api/reports` | reports.js | viewer+ / operator+ | **availability** (uptime % from probes) + **probe outages** list (`/probe-outages`, viewer+); CSV (`.csv`, gated `reports_csv`) + print-ready HTML→PDF (`.html`, gated `reports_pdf`); **NIS2 draft** (`/nis2-draft/:probe_outage_id`, operator+). UI **Reporting → Availability & outages** (`public/slaReports.js`: run, CSV, print, NIS2 draft and `POST /api/investigation/from-event` per outage) |
| `/api/report-schedules` | reportSchedules.js | viewer+ read · **admin** write · operator+ send-now | **SCHEDULED REPORTS** — the availability / probe-outage reports over a RELATIVE window (`window_days`, resolved at send time, so a stored fortnight is never re-sent forever), mailed to 1-20 recipients on the migration-099 recurrence. Delivery reuses the ALERTING SMTP settings (`services/reportMailer.js`), like the one-time-password mail, so an admin configures a mail server once. `POST /:id/send-now` runs the same build and the same send the timer does — a mail server nobody configured is **409 with the reason**, not 500 — and the outcome is stored as `last_run_status`, so a schedule failing for three weeks says so in its row instead of looking healthy. Creating one is ADMIN because it sends data out of the building on a timer, and every create/update/delete/send is audited WITH the recipients. Columns and rows come from `src/reports/definitions.js`, shared with the HTTP exports, so a mailed report and a downloaded one cannot disagree. See `docs/scheduled-reports.md` |
| `/api/service-tests` | src/serviceTests/api/ | viewer+ read · operator+ build/run · admin owns apps/credentials/allowlist | **SERVICE ASSURANCE** — applications · environments · credentials (write-only secrets) · **allowed-hosts** (the SSRF allowlist: host/ip/**cidr** + CSV import/export) · tests (+`/run` → 202, queued for the worker) · runs (+`/screenshot`, +`/evidence` — every observation one run made, assembled from already-masked columns; the run itself carries `performance`, a median+MAD verdict against the test's own successful history) · **map** (`GET /map?application_id=` — application → journey → test → page → endpoint, computed on read from what runs observed and read-only by design: a stored, editable map is the CMDB the spec forbids) · **stats** (`GET /stats?period=day|week|month|year&at=&tz_offset=&test_id=&application_id=` — per-bucket run counts for the history charts, aggregated in SQL; the calendar lives in `src/serviceTests/stats/period.js`, never in the browser) · **healing** (`GET /healing`, `POST /healing/:id/{accept,reject}` — self-healing selectors: when a step's target no longer resolves the runner records a PROPOSAL, and this is the only place in the module that rewrites a target from one. Scoring is `engine/heal.js`, pure; accepting re-checks the step still exists and still says what the proposal described, then saves through the ordinary test path so it gets a version bump) · discovery · suggestions (+`/accept`, `?kind=test|journey`) — Discovery proposes TESTS and the JOURNEYS they add up to (`suggest/journeys.js`, rules over the test suggestions rather than over the crawl again); accepting a journey suggestion creates its member tests, reusing any already accepted, then the journey that orders them · schedules · settings · **assurance** (`/assurance/incidents`, `/assurance/certificates`, `/assurance/summary`, `POST /assurance/incidents/:id/resolve`, `POST /assurance/certificates/check` — the reaction layer) · **journeys** (`GET/POST /journeys`, `GET/PUT/DELETE /journeys/:id`, `PUT /journeys/:id/steps` — User Journeys: the central V2 object. A journey ORDERS tests that already exist and owns none of its own, so every V1 feature works inside one on day one. Its verdict is computed, never stored: a broken *required* step fails the journey, a broken *optional* one degrades it, and every verdict carries the sentence explaining it — `src/serviceTests/journeys/health.js`, pure) · **recordings** (`GET /recordings`, `POST /recordings` → id + one-time capture token + bookmarklet, `GET /recordings/:id` → the translated definition, `POST /recordings/:id/{stop,accept}`, `DELETE /recordings/:id`) · **analysis** — the V3 intelligence layer, COMPUTED ON READ and read-only: `GET /analysis/runs/:id` (observations + correlation + ranked root causes, derived from the stored run when a pre-V3 run has no observations, and it says which), `GET /analysis/incidents/:id/recurrence` (has this happened before, and was it ever fixed), `GET /analysis/applications/:id/dependencies` (which endpoints several journeys share, and which of those are failing), `GET /analysis/applications/:id/health?hours=` (Service Health 2.0 — the score in its four published parts), `GET /analysis/tests/:id/anomalies?hours=` (failure rate, API errors and duration against this test's own history), `GET /analysis/ai/status`, `GET /analysis/incidents/:id/ai`, `POST /analysis/incidents/:id/ai` (operator+, audited — asking sends data to a third party). Nothing under `/analysis` is stored, so an analysis can never be more stale than its evidence; the exception is what correlation concluded, stamped on the incident when it opened · **incident lifecycle** (`POST /assurance/incidents/:id/status` — open → investigating → identified → resolved → closed, conditional on the status the caller was shown, so two browsers cannot silently undo each other; a refused move is 409 with a sentence). · **monitors** (`GET /monitors/types`, `GET/POST /monitors`, `GET/PATCH/DELETE /monitors/:id`, `POST /monitors/:id/check` → runs it now (409 while one is already running), `GET /monitors/:id/results`, `GET /monitors/:id/series` (availability per bucket for the history chart, over the same `stats/period.js` calendar the run charts use), `POST /monitors/:id/activate` (the activation override) — **the checks that are not a browser**: mail delivery (send-only, or a round trip that sends a tokened message and reads the destination mailbox back over IMAP and measures the delivery), DNS records (SPF/DKIM/DMARC/MX presets), blacklists (RBL), directory binds (LDAP), clock offset (SNTP), certificates on a non-web port, TCP ports and databases (a single SELECT, refused otherwise). Secrets are write-only; every host clears the same permanent deny-list a browser test does; a mail recipient must be in the operator's domain allowlist when one is set. A monitor is created PENDING and is not swept until one check comes back `ok`/`slow` — while pending a failure opens no incident, so a mistyped mail server cannot page anybody; `activate` is the override for when the service is down and that is exactly what you want watched. `/monitors/types` also serves the bounds the form is judged against (`limits`), and `PATCH { enabled }` is the pause. Every result stores its own trace — per-phase timings, the lists it recorded (DNS answers, the blacklists it asked and what each said), and for mail the SMTP conversation (credentials redacted), the `Received:` chain as a delivery path and every look in the probe mailbox — which the monitor page opens under the row, drawn generically so a new check type arrives with its findings visible. The phases are also charted across the recent checks, one panel per step scaled to itself (six phases on one axis is six magnitudes), with the pairs that move together named by rank correlation. Swept by its own job in the API process — `docs/service-assurance-monitors.md`) · Licence-gated as a whole (`service_tests`) behind `requireAuth`, so an anonymous request is 401 not 403 |
| `/api/service-capture` | src/serviceTests/api/recordings.js | **the capture token — no session** | **RECORDING INGEST.** `POST /events`, `POST /stop`. The one Service Assurance path outside `requireAuth`, because its caller is the recorder script on the CUSTOMER'S site, which has no BlueEye session and cannot get one. The token exists only because an authorised operator started a recording, is stored as SHA-256, resolves only while the recording is live and unexpired, and reaches exactly one row append-only; missing/unknown/expired/stopped all answer the same 401. CORS allows the origin, never credentials. See `docs/service-assurance-v2.md` §1 |
| `/api/thresholds` | thresholds.js | viewer+ read / admin write | **probe-outage thresholds** — global defaults + per-location overrides; `DELETE /?metric=` / `DELETE /:location_id?metric=` remove one (no threshold ⇒ the metric is not evaluated). UI **Settings → Outage thresholds** (`public/thresholdsPanel.js`) |
| `/api/event-clusters` | eventClusters.js | viewer+ read / operator+ write | **cross-agent event clusters** (`event_clusters`): `GET /` (filter status + `from`/`to`, paginated `limit`/`offset`) + `GET /:id` (hydrated members + evidence, confidence breakdown, suspected root-cause layer, evidence summary) + `POST /:id/ack` (op+, audited) + `POST /:id/resolve` (op+, required `note`, audited) + **read-only evidence** `GET /:id/evidence` (snapshots), `GET /:id/evidence/:sid` (raw text/plain), `POST /:id/evidence` (op+, manual re-snapshot, rate-limited `429`+`Retry-After`, evidence-class audit). Read-model built by `src/analysis/clusterView.js`; evidence capture in `src/evidence/` (`commandAllowlist.js` `evidence-v1` + `snapshotService.js` + `evidenceRetention.js`), table `cluster_evidence_snapshots` (mig 065). **Distinct** from `event_cases` — the task's `/api/events` path was taken, so clusters mount here. See `docs/cross-agent-correlation.md` |
| `/api/events` | events.js | viewer+ read / operator+ write | **EVENTS** (`event_cases`, wraps analysis findings) — the operator-facing unit; an *event* is what a connected ITSM opens FROM one (see `docs/events.md`). Responses carry `event`/`events`/`eventId` only — the deprecated `/api/incidents` alias and its duplicated `incident*` keys were removed (migration 077): **`GET /:id/notes`** (viewer+) + **`POST /:id/notes`** (op+) — the append-only **work log** for shift handover (`{text, kind}`, `kind ∈ observation|action|ruled_out`; `ruled_out` served as its own indexed array and pinned in the UI; audited, no PATCH/DELETE exists — see `docs/event-work-log.md`); `GET` (filter status/severity/device/time) + `GET /:id` (+ linked anomalies + a light `explanation` what/where/why). List + detail rows carry the device identity joined from `agents`+`locations` (`agentName`/`agentHostname`/`locationId`/`locationName`) so an event says WHERE it is. Also `GET /:id/timeline` + `GET /:id/config-context` (op+) + `GET /:id/similar` + `GET /:id/recommendation` (combined playbook→history→AI, `?force_ai=true` op+) + `PATCH /:id` (op+, state machine, hash-chained audit) + `POST /:id/ask` (op+, opt-in EU AI over masked/aggregated context). **Distinct** from the `probe_outages` surfaced by `/api/reports`. See `docs/events.md` + `docs/event-cases.md` |
| `/api/targets` | targets.js | viewer+ | **per-target (per-agent) event timeline**: `GET /:id/timeline?from&to&limit` — merges anomaly findings, probe outages, agent connect/disconnect (`audit_events`) and remediation playbook runs into one chronological (newest-first) list, each item `{ timestamp, source, type, severity, summary, ref_id }`. Default last 24h, `limit` capped 500. Read-only, no new tables. Partial-failure tolerant (one source down → `partial:true` + `failedSources`, not a 500). Pure merge in `src/timeline/targetTimeline.js` (reusable), fan-out in `src/timeline/targetTimelineService.js`. See `docs/target-timeline.md` |
| `/api/diagnose` + `/api/playbooks` | diagnose.js | viewer+ read · **operator+** run/evaluate | **SYMPTOM-FIRST DIAGNOSIS** — a technician writes what is wrong in their own words and gets a plan: the likely causes ranked, the tests to run with their parameters already filled in, the screens to open and what to look for in each, and what each answer would mean. `POST /diagnose` (describe → plan; 1000-char bound), `GET /diagnose/:id` (plan + per-test status + last evaluation), `POST /diagnose/:id/run` (dispatches the plan's tests as ordinary `run-probe` commands), `POST /diagnose/:id/evaluate` (applies the playbook's reading rules and marks every cause **confirmed** / **ruled out** / **open** — with the rule, the sentence behind it and the measurement that decided it), `GET /playbooks(/:id)`. The knowledge is DATA (`src/diagnose/playbooks/*.json`) so a new playbook is a new file; the LLM may only CHOOSE from that catalogue and anything it invents is discarded; **verdicts are decided in code**, never by a model, and the AI only writes the summary afterwards. Reading a plan is viewer+ (a POST because a description is a paragraph); running and evaluating are operator+ because one makes the network do something and the other can send context to a third party. Audited under category `diagnose`. **Distinct from `/api/diagnostics`** (admin, outbound-connectivity test area). Sessions in `diagnose_sessions`/`diagnose_session_tests` (mig 097). See `docs/diagnose.md` |
| `/api/troubleshooting` | troubleshooting.js | viewer+ (filtered by role) | **consolidated Troubleshooting Dashboard** — `GET /overview?minutes=&limit=` returns the whole screen in ONE read: `summary` (activeFaults/affectedDevices/rootCauses/anomalies + devicesDown/Unreachable), `topology` (nodes+links with state `ok`/`degraded`/`down`/`unreachable_downstream`, `layer` l2/l3, plus admin-only `discovered[]`; `degraded` = reachable with an open CRIT/WARN fault on it, a switch-port finding marking the switch), `rootCauses[]` (the correlator's rollup — one cluster = ONE cause — PLUS one cause per open event case outside a live situation, `source: 'cluster'|'case'`, so a single-agent site that never forms a cluster still shows its faults; findings a live cluster lists are never counted again under a case; with `blastRadiusCount`), `anomalies[]` (flow-pair baseline deviations) and `timeline[]`. Owns **no tables** — a read/aggregation layer over topology rediscovery + dependency mapping + blast radius + flow-pair baselining + active discovery. Pure read-model `src/troubleshooting/overview.js`, fan-out `overviewService.js` (Promise.allSettled → `partial`+`failedSources`; takes the topology graph ONCE via `blastRadiusService.graph()` and runs the pure `computeBlastRadius` per node). RBAC is viewer+, and each source is included only for the roles that may read it on its own (a viewer gets no flow baselines, topology changes or blast radius; `restricted` names them) — aggregating never widens access. Read-only: no agent command, no audit write. `GET /faults?limit=&offset=&clusterId=&caseId=&source=` is the OPT-IN drill-down behind `summary.activeFaults` — the raw member findings, paged (limit 1..500), fetched only when the operator asks to list them; `/overview` never carries them and hydrates members through ONE narrow `FindingStore.listByIds(ids, {light:true})` bulk read instead of one `get()` per member. See `docs/troubleshooting-dashboard.md` |
| `/api/devices` | deviceConfig.js | operator+ | **device config history**: `GET /:id/config-history` (masked snapshots + risk-classified diffs) + `POST /:id/config-snapshots` (manual ingest of a raw config capture) |
| `/api/fleet` | fleet.js | viewer+ | **fleet health** rollup (`/health`) + per-agent verdict (`/agent/:id`) + **NIC firmware inventory / drift** (`/nics`) |
| `/api/dashboard` | dashboard.js | viewer+ (gated `dashboard_advanced`) | Overview **“open issues”** rollup (`/advanced`): **open `event_cases`** + probe outages + recent findings, surfaced inline on the Overview, Professional+ |
| `/api/interfaces` | interfaces.js | viewer+ | **interface health** (util/errors/discards/link) |
| `/api/device-events` | deviceEvents.js | viewer+ | **DEVICE LOG** — what the network equipment itself said (syslog now, SNMP traps next). `GET /` (window + severity/device/type/transport/free-text filters, newest first, with the severity counts the chips read) + `GET /catalog` (the severity names + event-type groups the filters offer, served so the dashboard and the stored data cannot drift). Owns no analysis: a read over `device_events`. Ingest is `POST /agents/me/device-events` (agent token) — **SNMP traps arrive on the same route with `transport:’trap’`**: no new table, no new route, no new screen, which is what migration 103’s two-value ENUM was for. See `docs/device-events.md` |
| `/api/snmp-devices` | snmpDevices.js | viewer+ read · **admin** write · operator+ poll | **SNMP DEVICE INVENTORY** — the switches an agent polls ON THE SERVER’S BEHALF, which is what breaks the old 1:1 `monitor_config.source=‘snmp’` binding (one agent now covers a wiring closet). CRUD + `POST /:id/poll` (correlated poll-snmp: 200 with the agent's result, 202 if it has not answered within 20 s). The community string is AES-256-GCM at rest and **never returned**; `host` is checked against the SSRF deny-list twice, on write and again before a poll is handed out. Ingest is `POST /agents/me/snmp-topology` (agent token), which refuses any device the submitting agent does not poll. See `docs/snmp-topology.md` |
| `/api/snmp-devices/:id/interfaces` | snmpDevices.js | viewer+ | **THE PORT TABLE** on one polled switch (migration 108) — name, alias, ifIndex, admin/oper status, speed, MAC. Served on its own as well as inside `GET /api/snmp-devices/:id`. 404 for an unknown device rather than an empty list |
| `/api/snmp-devices/:id/counters` · `/:id/interfaces/:ifId/series` | snmpDevices.js | viewer+ | **INTERFACE COUNTERS** (migration 109) — the newest sample per port with its name/alias/speed, and one port's series over a window (downsampled to 500 points). **503**, not an empty list, when counters are not configured: “not collecting” and “collecting, nothing seen” send a technician to different places. Ingest is `POST /agents/me/snmp-counters` (agent token) |
| `/api/snmp-profiles` | snmpProfiles.js | **admin**, including the read | **NAMED SNMP COMMUNITIES** (migrations 112 + 113), what Settings calls *SNMP communities*. CRUD + `/meta` (the auth/priv protocols this build can use, so the UI cannot drift from the validator) + `?locationId=`/`?agentId=` (which communities are valid here / which may this agent walk with) + `PUT /order/:locationId` (the SITE's order of preference, which must name exactly the communities assigned there). A community is assigned to SITES (`locationIds`, ordered) and to AGENTS (`agentIds`) — an agent walks only with a community assigned to it. NO SECRET IS EVER RETURNED — the reads say WHETHER one is set and which protocols, never the keys. Every change is audited with the FIELDS that changed and not their values; a delete reports how many devices fall back to the chain |
| `/api/coverage` | coverage.js | **admin** | **COVERAGE GAPS** — "which parts of the network do I NOT see". `GET /?limit=1..200` (rows per gap kind; summary counts all) returns `{summary, gaps[], truncated, checks[], windows}`; each gap `{kind, severity: info\|warn, scope: site\|agent\|device\|subnet, subject, evidence, suggestion, link}`. Sites with no agent / no flow source and no switch / no flows in 24 h; agents offline, not reporting (30 min), proc-only, flow source silent 24 h; switches disabled / unpolled / never answered / failing / no credential (`resolveForAgent`) / not collecting ifcounters, LLDP, FDB; unknown MACs on up non-LLDP ports per switch; LLDP neighbours (`snmp_neighbors` + `lldp_neighbors`) nothing monitors; ARP /24s with no agent; unreviewed discovery candidates. Owns **no tables**: pure rules `src/coverage/coverageGaps.js` + gatherer `coverageService.js` over existing repos (new reads `flowsRepo.lastFlowAtByAgent`, `arpEntriesRepo.subnetSummary`/`macsForIps`, `fdbEntriesRepo.listUpPortMacs`). A source that cannot be read makes its checks `skipped` — never clean. Admin: a list of blind spots is a map of where nobody looks. UI **Administration → Coverage gaps** (`public/views/coverage.js`, `coverage.*` strings). See `docs/coverage.md` |
| `/api/topology/l2-path` · `/api/devices/locate` · `/api/devices/inventory` | l2Path.js | viewer+ · viewer+ · **operator+** | **L2 PATH & DEVICE LOCATION** — "which switches and ports does A cross to reach B" and "where is X plugged in". An endpoint is an IP, MAC, hostname or `agent:<id>` (`src/validation/l2PathValidation.js`). The path returns `segments[].hops[]` (switch, ingress/egress port with oper status, speed, VLANs, newest error/discard/utilisation) plus `uncertainties[]` with evidence (`missingLink` = "an unmanaged switch or missing LLDP between X and Y", `differentVlans`, `gatewayUnknown`, `notInFdb`, …); split at the gateway when the VLANs differ and one can be placed (`gateway=` or a router ARP table). 400 bad endpoint · 404 unknown endpoint (named) · 503 no forwarding table on this install. Inventory is operator+ (every host and where it is plugged in, paged `limit` 1..200). See docs/l2-path.md |
| `/api/burst` | burst.js | viewer+ read · **operator+** start/stop | **BURST MODE** — one target, once a second, for up to two minutes, on demand. Fills the gap the 60-second reporting interval and the hourly baselines leave: a five-second loss event is invisible to everything else here. `GET /` (recent runs, without their samples) · `GET /:id` (one run WITH its series) · `POST /` (202 — the agent measures; 409 + the recorded `runId` when it is not connected) · `POST /:id/stop` (202, stops at the next tick). The verdict — whether the loss is EVEN or CLUSTERED, which is the diagnostic value a percentage cannot carry — is computed once in `src/probes/burstAnalysis.js` (median + MAD, no model) and STORED, so the row reads the same in a report six weeks later. Starting is operator+, not admin: the person in front of the fault at 02:00 is usually not an admin. See `docs/burst-mode.md` |
| `/api/baselines` | baselines.js | viewer+ | **baseline context for display** — `GET /flow-pair?host=` returns the day-of-week/hour flow-pair volume baselines (mig 068) the shared UI metric component compares against, incl. `observationCount`/`updatedAt` for the hover age; `&dst=&port=` narrow to one pair, and `current` is the last rolled-up hour (`{bucket, dow, hour, pairs[]}`) the UI compares with its slot (agent page → Dependencies "Hour hh:00 UTC" column + Baseline dialog). `building:true` (not 404) when a host has none yet — normal for ~14 days after install, since history builds FORWARD. viewer+ is deliberate (same data class as the viewer-visible Flows explorer, with a comparison attached); the operator+ `/api/topology/flow-baselines` diagnostic route is untouched. See `docs/baseline-context.md` |
| `/api/changes` | changes.js | viewer+ | **"what changed since I last looked"** — the dashboard's LANDING page (the fleet grid moved to its own route). `GET /?since=<iso\|last_login>&window=&limit=&offset=` merges agent connect/disconnect, new anomalies, probe outages (**degrade AND recover**), events, situations, topology link changes, playbook runs and config captures into one severity-grouped, newest-first feed in the shared timeline event shape. `POST /seen` moves the per-user marker (`users.last_seen_changes`, mig 074) — **the only thing that does**; a GET never advances it, and the write is monotonic so a stale tab cannot rewind it. `POST /ack {key}` / `DELETE /ack/:key` acknowledge one row for the caller only (`change_acks`, mig 115): every row carries `ackKey` (hash of the condition, `ackKeyFor`) + `acknowledgedAt`; a new occurrence after the ack re-opens the row; DELETE with nothing to undo is 404. `POST /mute {key, hours}` / `DELETE /mute/:key` mute a RULE (`muteKey` = hash of source + type, every host) for the caller, 1..168h (default 24), `change_mutes` mig 134; rows carry `mutedUntil`; never touches alerting. **Correlated before capping:** an anomaly whose event row is on the feed is folded INTO it (`findingCount`), and repeats of one condition on one device collapse to a single row with `count`/`firstAt`/`refIds` — key `kind|source|type|agentId|metric|severity`, so a fold never crosses device, condition, severity or direction; `rawTotal`/`correlated` report what was folded. Each row carries a `family` (`src/changes/indications.js`) the UI renders as "what this indicates". Heartbeat/version-skew rows are flagged `currentState` (neither is transition-logged, so we report the condition and refuse to invent a start time). Empty window = 200 + the reference times, never 404. Partial-failure tolerant. Owns no tables beyond the marker column, `change_acks` and `change_mutes`. See `docs/changes-feed.md` |
| `/api/search` | search.js | viewer+ | **universal search** — one field taking whatever the technician knows (IP · MAC · hostname/DNS · site · agent id · service/port · CMDB asset · discovered device). Returns ONE ranked `hits[]` where every hit carries `type`/`display_name`/`target`/`confidence`/`source`/`last_seen` — provenance + age ride on every row so a three-week-old ARP answer is distinguishable from this morning's. IP/MAC match exactly (MAC normalised through the SAME function the ARP ingest parser uses, so five spellings resolve identically); names match exact/prefix/substring. Ordered by confidence → `last_seen` desc; partial-failure tolerant (`partial`+`failedSources`); rate-limited **per user**; the username resolver is a documented empty stub (no end-user identity source exists — reported in `unresolved`). Pure query analysis `src/search/query.js`, fan-out `src/search/searchService.js`. See `docs/universal-search.md` |
| `/api/integrations` | integrations.js | admin | **outbound API integrations** (ITSM ticketing + Nautobot CMDB/IPAM sync + generic/`custom` config-driven connectors): CRUD + `/meta` (types + category + presets) + manual test-fire; credentials encrypted at rest. UI = **Settings → ITSM** (`settingsIntegrationsView`) |
| `/api/diagnostics` | diagnostics.js | admin | **Test area** — consolidated connectivity + security screening of every outbound integration (email/alert channels, ITSM/IPAM, SSO, AI/map/licence). `/targets` (catalogue + posture) + `/run`. Reuses each subsystem's own test primitive; each UI row deep-links to its setup. No secrets returned |
| `/api/settings/security` | authSecurity.js | admin | **Baseline security policy** (migration 041, never licence-gated): password history depth, opt-in password max age, role-based IP allowlist. PUT refuses (409) an admin allowlist that excludes the saving admin's own address. Enforced by `src/auth/securityGate.js` on every authenticated request (+ sign-in, SSO callbacks, dashboard WS). See docs/security-hardening.md |
| `/api/ldap` | ldap.js | admin | **LDAP/AD auth** config + group→role map + connectivity test + login audit (`/login-audit`); writes licence-gated (`sso_ldap`, Professional) |
| `/api/oidc` | oidc.js | admin | **SSO (OIDC)** status + claim→role map + discovery test + login audit; writes licence-gated (`sso_oidc`, Professional). Connection from env vars |
| `/api/saml` | saml.js | admin | **SSO (SAML)** status + attribute→role map + login audit; writes licence-gated (`sso_saml`, Professional). Connection from env vars |
| `/api/audit` | auditEvents.js | admin | **Reporting → Audit** + **Administration → User Logs** — unified server-wide audit trail (user actions + agent activity); `/` (filterable list), `/all` (both stores merged), `/actions` (distinct), `/export.csv`, and **`/users`** + **`/users/export.csv`** (the User Logs read model: user-caused rows only, resolved to `{userId, name, email}` against the live users table, described in plain language and flagged — `?user=&flagged=&q=&from=&to=&limit=`). Flag rules are pure in `src/audit/userActivity.js`. **`/users` is NOT licence-gated** while `/all` and `/api/audit-log` are: User Logs IS the audit record, and one that drops rows by plan is untrustworthy — the `audit_log` feature sells the tamper-evident compliance API over the same rows, not an admin's view of their own users' activity. Writes come from the audit middleware + agent ingest, not HTTP |
| `/api/nis2` | nis2/ (index · risks · controls · incidents · evidence · reports · generator · exports · audit · meta) | viewer+ read / operator+ write / admin approve | **Reporting** (UI tab) — **NIS2** module: `/dashboard` (readiness score + category status + top actions), `/risks` `/controls` `/events` `/evidence` (CRUD), `/incidents/from-event-case/:caseId` (draft from an event case, operator+), `/deadlines` (Art. 23), `/reports` (+ `/:id/approve`), `/audit` (admin), `/seed` (starter controls), `/export/*.csv` + `/export/*.html` (print→PDF); **Report Generator**: `/custom-reports/sources` + `/custom-reports/{preview,export}` (selector-driven custom reports, HTML/CSV/JSON) |

## Data model (MySQL)

Core tables (`schema.sql` + early migrations): `users`, `locations`, `agents`,
`agent_tokens`, `enrollment_codes`, `results` (one JSON `payload` per measurement).
Later migrations add:

| Migration | Table | Used by |
| --- | --- | --- |
| 009 | `findings` | analysis |
| 010 | `flow_records` | geo (enriched flows) |
| 011 / 012 | `flow_rollup` / `metric_rollup` | retention down-sampling |
| 013 | `app_settings` | runtime-editable settings (key/JSON) |
| 014 | `probe_results` | active probes (`mtu` JSON added in 096 — the path-MTU verdict) |
| 015 | (index only) | `idx_probe_ts` for the fleet-wide probe scan |
| 019 | `probe_results.status` / `.cert_expiry_days` | http probe (status + TLS cert expiry) |
| 032 | `probe_results.bytes` / `.content_type` | curl content-check probe (received byte count + content-type; pass/fail rides on `detail`) |
| 033 | `probe_results.elements` | pageload probe waterfall — JSON `[{url,kind,status,bytes,ms}]` (rtt_ms = total load time, bytes = page weight) |
| 020 | (column) | per-user UI preferences — `users.preferences` JSON (colour theme) |
| 021 | (column) | `agents.enrollment_code_id` → links an agent to the code it enrolled with (Enrollment page shows each code's agents + live status); `ON DELETE SET NULL` |
| 022 | `agent_action_audit` | server-initiated agent actions (upgrade/delete) |
| 024 | `probe_thresholds` | event derivation — per-metric warn/crit + debounce; global default (`location_id` NULL) + per-location override; seeded |
| 025 | `events` | event derivation — one row per (agent, metric, target) outage; `started_at`/`resolved_at`/`duration_seconds` |
| 026 / 027 | `integrations` / `integration_audit` | outbound API integrations + per-fire audit (credentials encrypted at rest) |
| 028 / 029 | `ldap_config` + `ldap_role_map` / `ldap_login_audit` | LDAP/AD auth config + group→role map + login audit (bind password encrypted at rest) |
| 031 | `blueeye_nis2_risks` · `blueeye_nis2_controls` · `blueeye_nis2_incidents` · `blueeye_nis2_reports` · `blueeye_nis2_evidence` · `blueeye_audit_log` | **NIS2 Reporting Center** — risk register, control evidence, security incidents, generated reports (with frozen metric snapshots for trend), evidence references + a generic change audit log |
| 033 | `audit_log` | **unified audit log** (feature `audit_log`) — auth/user/role/license/report/api_token events; metadata only, never secrets |
| 034 | `api_tokens` | **API tokens** (feature `api_access`) — programmatic access; only the SHA-256 hash stored, role-scoped |
| 035 | `audit_events` | **unified, server-wide audit** (Reporting → Audit) — user actions (audit middleware) + agent activity (on ingest); recurring activity folded onto one row via a nullable UNIQUE `dedup_key`. Agent activity includes `agent.probe-failed` — a probe the agent could not execute (e.g. `traceroute` missing), with the reason in `detail.reason` — `agent.error`, a non-fatal operational error the agent reports over the WS channel (recorded in `src/ws/agentSocket.js`, deduped per category) so agent-side failures are visible server-side, and the agent **lifecycle**: `agent.enrolled` (new agent via a code, in `src/routes/agentEnroll.js`) + `agent.online`/`agent.offline` (each WS connect/disconnect, discrete rows with source IP) |
| 036 | (column) | extends `agent_action_audit.action` with `'install-tool'` — operator- or auto-triggered request that an agent install a missing diagnostic tool (request→complete like upgrade/delete; tool in `target_version`). See `src/agentTools.js`, `services/installToolService.js` |
| 051 · 052 | `cmdb_config` · `agent_cmdb_links` | **CMDB integration** (single source of truth) — one singleton connection config for ONE CMDB source (ServiceNow, Nautobot, **or a config-driven `custom` HTTP/JSON CMDB** — migration 052 adds the `custom` type + `config_json`; credentials AES-256-GCM at rest via secretBox, never returned) + per-agent asset links (`agent_id` PK, FK cascade on agent delete; captures the asset's CMDB location, which **syncs the agent's site** — `agents.location_id` — by matching a BlueEyes location by name, creating one if absent). Reuses the integrations connector registry for the connection test + asset search |
| 046 | `transaction_tests` · `transaction_test_agents` · `transaction_results` · `transaction_baselines` | **transaction tests** (http/tcp/dns/icmp journeys) — normalized: one test row (type ENUM + target + JSON config incl. thresholds + AES-256-GCM `config_secrets`), an agent-assignment join, one result row per run (`time`, status ENUM, `step_timings`, `step_failed`, `deviation`, structured `detail`; **no FKs** — TimescaleDB-bound), and per-(test,agent,step) median+MAD baselines recomputed hourly (leader-only, `src/analysis/transactionBaselines.js`, reuses `analysis/baselines.js`). Config pushed + results ingested over WS. Supersedes the dead-in-prod migration 045 (dropped here) |
| 047 | `event_cases` | **events as a first-class entity** wrapping analysis findings — status (open/investigating/resolved/closed) + severity + auto-title, `primary_finding_id`→findings, `config_change_id`→config_snapshots (mig 050), `closed_by`→users, `created_by` system/manual. **Distinct** from `probe_outages` (mig 025) |
| 048 | `findings.event_case_id` | nullable FK linking an anomaly (finding) to its event case (`ON DELETE SET NULL`) — the grouping key |
| 049 | `config_snapshots` | raw device-config captures (`device_id`→agents, `config_text`, `captured_at`, `captured_via` manual/agent_poll/change_detected); operator/admin + secret-masked on read; retention via `RETENTION_CONFIG_SNAPSHOT_DAYS` (default 180d) |
| 050 | `event_cases.config_change_id` | nullable FK → `config_snapshots` — the config change auto-correlated (within a configurable window, default 30m) as the suspected trigger |
| 053 | (retire) | **retire Enterprise + HA** — drops the `ha_nodes` table (HA removed) and reconciles `license_plans`/`licenses` after the plan catalogue collapsed to Pilot/Starter/Professional (Enterprise + MSP retired) |
| 056 | (columns) | **local user creation with a one-time password** — `users.must_change_password` / `.temp_password_expires_at` / `.temp_password_created_by` (FK→users, ON DELETE SET NULL). See docs/user-local-otp.md |
| 057 · 058 | `event_clusters` (+ `advisory` col) | **cross-agent event clusters** — findings from ≥2 agents grouped with a confidence tier + suspected common cause; `advisory` holds the opt-in cluster-level AI root-cause suggestion. See `docs/cross-agent-correlation.md` |
| 060 | `event_clusters` lifecycle cols | **cluster operator lifecycle** — `status` enum gains `acknowledged`; adds `acknowledged_at`/`acknowledged_by`, `resolved_by`, `resolution_note` (`*_by`→users, ON DELETE SET NULL). Backs the ack/resolve REST API |
| 061 · 062 | `runbooks` · `verification_runs` | **recommended actions + verification loop** — finding-type→markdown runbooks (optional linked playbook); post-remediation verification runs (re-check after settle, never auto-resolve). See CHANGELOG 0.81.0 |
| 063 | `lldp_neighbors` | **LLDP neighbor graph** — persisted L2 adjacencies (`local_agent_id`/`local_chassis_id`/`remote_chassis_id`…) fed by the agent `capabilities.lldp` report; a topology fallback for cross-agent clustering (site wins). Graph in `src/topology/lldpGraph.js` + cached `lldpGraphService.js`; API `GET /api/topology/neighbors`. See CHANGELOG 0.82.0 |
| 068 | `flow_pair_hourly` · `flow_pair_baselines` | **per-flow-pair volume baselines** — extends per-metric anomaly detection to per-`(src,dst,port)`. Append-only hourly rollup (fed by `tcpServiceFlows`+resolver; builds forward) → day-of-week/hour-of-day median+MAD baselines (reuses `analysis/baselines.js`, no new stats), min-100-observation gate. Leader-only hourly job `src/analysis/flowPairBaselineJob.js` scores + emits deviations as findings (`metric flow.volume`) via `findingStore.save`. API `GET /api/topology/flow-baselines`. See CHANGELOG 0.90.0 |
| 069 | `discovered_devices` | **scheduled active discovery** — native-Node scan (TCP connect + rDNS; ICMP injectable/unsupported) of admin-configured CIDR scope, rate-limited, address-capped, refuses out-of-scope/over-cap. Candidates require admin promotion → SNMP `agents` row (never auto-enrol). Every sweep audited (hash-chained). Admin-only router `/api/discovery/*`: `GET/PUT /config` (scope is **runtime-editable** via `settingsService.getDiscovery/setDiscovery`, overlaying the `DISCOVERY_*` env defaults; `enabled`/schedule stay env), `GET /candidates` + `/:id/promote` + `/:id/ignore`, `POST /scan` (with `agentId` → pushes a `run-discovery` command so the AGENT sweeps from its own vantage; without → server-side sweep), `GET /sweeps` (from the audit log). Agent-executed candidates return via agent-authed `POST /agents/discovery-results` (upsert + `found_by_agent_id`, mig 071, + audit). Agent side `blueeye-agent/src/discovery/` + the `run-discovery` command. The sweep job re-reads scope each run via a `getConfig` provider. UI = **Administration → Discovery** (`views.discovery`, admin-gated). Engine `src/discovery/`. See CHANGELOG 0.90.0 |
| 067 | `topology_changes` (+ `lldp_neighbors.link_state`) | **topology change detection** — LLDP neighbour changes between poll cycles (`neighbour_added`/`neighbour_removed`/`link_state_changed`/`port_moved`) with flap suppression (revert within `TOPOLOGY_FLAP_WINDOW_SECONDS`, default 300, → one `flapping` record). Detected at `POST /agents/me/capabilities` ingest (diff before upsert), recorded reusing the target-timeline event shape (new `topology` source), and written to the hash-chained `audit_log` as evidence. Repo `topologyChangesRepository.js`; engine `src/topology/topologyDiff.js` + `topologyChangeService.js`; API `GET /api/topology/changes`. See CHANGELOG 0.89.0 |
| 070 | `host_connections` | **agent connection-table edges** — a second SOURCE for the service dependency graph, for hosts with no flow exporter. The agent reads its OWN established TCP connection table (`ss`/`netstat`/Get-NetTCPConnection — metadata only) and reports directed `(src_ip→dst_ip:dst_port)` edges in `capabilities.connections`; ingested at `POST /agents/me/capabilities` (raw, kept out of the agents JSON) via `hostConnectionsRepository.replaceForAgent`. The service-dependency job merges these (byte-less, `conn_count` only) into the SAME aggregator as the flow rows — identical IP→host resolution + unknown-endpoint drop. Repo `hostConnectionsRepository.js`; agent side `blueeye-agent/src/connTable.js`. See CHANGELOG 0.94.0 |
| 066 | `service_dependencies` | **service dependency graph** — directed host↔host TCP edges aggregated over a rolling 24h window by `(src_host_id, dst_host_id, dst_port)` with byte/packet/conn counts + first/last-seen. The `service_dep` edge type of the unified topology graph (`l2_link` = LLDP). Recomputed by a leader-only scheduled job (`src/topology/serviceDependencyJob.js`) off the ingest hot path; IP→host resolution via `capabilities.ips` / SNMP `monitor_config.snmp.host`, unknown endpoints dropped. Repo `serviceDependenciesRepository.js`; API `GET /api/topology/{dependencies,graph}` + `POST /dependencies/recompute`. See CHANGELOG 0.87.0 |
| 059 | `alert_dispatch_log` | durable record of dispatched alerts (finding + cluster) — lets a cluster alert fire once and reference (not resend) member-finding alerts |
| 064 | `event_clusters` rollup refs | **cluster-level notification rollup** — `alert_last_at`/`alert_last_severity`/`alert_member_count`, `itsm_ticket_ref`/`itsm_integration_id` (one ITSM ticket per cluster), `nis2_draft_id` (one NIS2 draft per cluster). See CHANGELOG 0.83.0 |
| 065 | `cluster_evidence_snapshots` | **read-only evidence snapshot on cluster open** — one gzip blob per (cluster, target) captured over the agent-command path (`iface.counters`/`arp.table`/`snmp.reads`/`agent.state`, allowlist `evidence-v1`); `items` records each command's outcome; `status` pending/complete/partial/failed/agent-offline; `trigger` auto/manual. Evidence, **not** time series (never TimescaleDB). Retention skips clusters with an unacked CRIT (default 90d). Engine `src/evidence/`. See CHANGELOG 0.84.0 |
| 093 | (column) | `users.name` — a display name on the account, so **User Logs** can say who did something instead of printing an address. Display ONLY: never an identifier, never unique, never used to look a user up; the email stays the key and NULL means "no name given", with every reader falling back to the email. The one-time-password invite already accepted a `name` for its email greeting and discarded it — this is where it goes |
| 097 | (column) | `probe_results.sizes` — the ping don't-fragment size sweep, one entry per payload size asked for in one run. Migration 096 gave `path_mtu` its own column; this is the other half of seeing an MTU fault, and a different measurement: the SAME target at several sizes. The row's OWN rtt/loss columns describe the SMALLEST size, so a blocked 1472-byte packet never reads as an outage to availability, fleet health or the detector — only this column carries the size dependence. `measured:false` marks a payload the LOCAL stack refused, which never reached the wire and is not loss on the path |
| 098 | `diagnose_sessions` · `diagnose_session_tests` | **symptom-first diagnosis** — one row per "describe the problem", with the description as written, WHICH matcher produced the plan (`keywords`/`llm` — the UI has to be able to say so), the resolved entities, the frozen `plan` and the last `evaluation`. The per-test rows carry the correlation this module needs and could not otherwise have: `probe_results` has no run id, so a session finds its own results by `(agent, type, target, ts >= dispatched_at)`, and `probe_result_id` makes the evidence link exact once found. See `docs/diagnose.md` |
| 099 | `test_packages.schedule_spec` | **a test package can repeat on a CALENDAR, not just on an interval** — `schedule_ms` says "every N ms since the last run", which cannot express a time of day and cannot reach past 24 hours, so weekly and monthly were impossible. The spec (`{period, every, at, weekday?, dayOfMonth?}`) is the calendar half, stored BESIDE the interval: a package has one or the other (saving a spec zeroes `schedule_ms`), so the scheduler never has to choose between two answers. `every` is the number of runs INSIDE one period, evenly spaced from the anchor. The maths is `src/schedule/recurrence.js` — pure, server-local time, walking the calendar rather than adding constants, which is why a daily 08:00 stays at 08:00 across a DST change and a monthly one spaces itself over the month's real length. See `docs/connection-test.md` |
| 100 | `report_schedules` | **reports that arrive on their own** — which report, how long a window (RELATIVE, in days back), for whom, how often (the 099 recurrence), plus `last_run_at`/`last_run_status`: the honest half, so a send that has been failing is visible on the screen that created it. Ticked by `src/services/reportScheduler.js` as a background job. See `docs/scheduled-reports.md` |
| 101 | `probe_results.tls` · `.rdns` | **the certificate a port presents, and what the address says it is called** (agent 0.27). `tls` keeps the four faults APART — expiry, chain trust (with node's own `authorizationError` code verbatim), hostname match, protocol/cipher — because they have four different fixes; `hostnameMatches` is TRI-STATE, where null means there was no name to check (an IP with no SNI) and coercing it to false would report every IP target as serving the wrong certificate. `rdns` carries the PTR names and, separately, **`forwardConfirmed`**: a PTR that does not resolve back to its address looks fine until somebody checks it, and mail servers check. Both types join `path_mtu` in DIAGNOSTIC_TYPES — an expired certificate is not a reachability outage and must not move an SLA number. See `docs/probe-tls-rdns.md` |
| 103 | `device_events` | **DEVICE EVENTS** — what the equipment itself says: syslog received by an agent on the customer's own network (udp+tcp 1514, off by default), and SNMP traps from stage 03 on the same rails. `transport` is an ENUM with BOTH values from the start — a trap and a syslog line are the same thing over a different socket. No FKs: TELEMETRY per docs/storage-split-audit.md, so it is dual-store (MySQL + a TimescaleDB hypertable) like `results`. `device_id` is nullable because an unresolved sender is STORED, not dropped. `clock_skew_ms` is a column because a switch three seconds out ruins every correlation silently. Nullable UNIQUE `dedup_key` carries a 5-minute bucket, so folding can never reach across windows |
| 104 | `snmp_devices` | **the switches the server polls**, and through which agent. NOT an `agents` row: a polled switch has no token, heartbeat, version or self-update, and modelling it as an agent would mean teaching every fleet rollup and licence count to exclude it. `agent_id` is nullable + ON DELETE SET NULL — the inventory outlives whichever host polled it. `community_encrypted` is secretBox (AES-256-GCM) and never leaves the repository; `supported` is what the device ANSWERED, NULL until it has |
| 105 | `fdb_entries` | **which switch port a MAC is on** — the last hop `arp_entries` (073) could not reach. Stores `bridge_port` as the device said it AND the resolved `if_index`/`if_name`, because **bridge port is not ifIndex** and a wrong resolution is right in the lab and wrong in the building. VLAN is in the unique key (Q-BRIDGE learns per VLAN; vlan 0 means the device did not say). `port_mac_count` is what turns a hit into an answer: 1 = an end device, 40 = an uplink. Ageing, not history — an entry ages out of the SWITCH in minutes |
| 106 | `snmp_neighbors` | **LLDP as seen BY a switch.** Deliberately NOT `lldp_neighbors` (063): that keys on an `agents` id and these rows belong to an `snmp_devices` id, so reusing it would attribute a switch’s neighbours to whichever agent shared the number — which does not throw, it draws the wrong network. The merge into the topology graph is left as its own decision |
| 107 | `burst_runs` | **one target, once a second, on demand** — the resolution the 60-second reporting interval and the hourly baselines cannot give. The samples are a JSON COLUMN, not a table: a burst is at most 240 points, bounded, written once and read as a whole — a small field, not a time series. `probe_results` is the opposite case and stays as it is. The verdict is stored beside them, because the explanation travels with the measurement here; `requested_seconds` keeps what was ASKED for beside what ran, so a run the agent clamped is explainable rather than suspicious |
| 108 | `device_interfaces` | **the ports on a polled switch**, as their own inventory — the join every per-port measurement needs, and the list the agent had been sending on every poll since stage 02 while the ingest threw it away. `UNIQUE (device_id, if_name)`: **the NAME is the identity, not ifIndex**, which the MIB only guarantees stable between re-initialisations — a reboot or a new module renumbers, and a time series keyed on it silently mixes two different physical ports. `if_index` is a mutable attribute, `if_index_changed_at` records the move, and the upsert REPORTS it so the counter path can mark that cycle discontinuous. `name_source` says which OID the name came from, so a row built from the weaker ifDescr is visibly weaker. NOT `interface_states` (088): that keys on an `agents` id with ON DELETE CASCADE, and a switch is not an agent |
| 109 | `device_counter_samples` · `snmp_devices.counter_interval_sec` | **what a switch port has been doing** — the first per-port time series here. Stores the RAW counter AND the derived rate: without the raw value a rate can never be recomputed, a counter reset can never be recognised after the fact, and a missing cycle cannot be told from a cycle that measured zero. `discontinuity` (first/reboot/renumber/gap/wrap) is why a rate is absent — in each of those cases the subtraction produces a number that is a lie, which is worse than a gap because it is indistinguishable from a measurement. `interface_id` points at a `device_interfaces` row (the NAME), never at an ifIndex. TELEMETRY: dual-store, and the first table in the schema with a TimescaleDB COMPRESSION policy. See `docs/device-counters.md` |
| 110 | `findings.device_id` · `.interface_id` | **a finding can name a SWITCH PORT**, not only an agent. Until this, `host_id` was always an agent and a port dropping frames had nowhere to go — the alternatives were smuggling device+port into a string (which destroys every join to `agents`, silently) or a second findings table (two places to look for “something is wrong”, which is what `event_cases` exists to prevent). Both columns are NULLABLE and a device finding ALSO carries the polling agent in `host_id`, so every existing per-agent read still finds it and nothing needed backfilling |
| 111 | `fdb_entries.prev_bridge_port` · `.move_count` · `.last_move_at` | **a MAC that MOVED leaves a trace** — the signature of a forwarding loop. The upsert used to rewrite `bridge_port` in place, so two sweeps of a switch in a loop looked exactly like two sweeps of a quiet one: the evidence was arriving and being overwritten. Recorded IN the upsert's ON DUPLICATE KEY UPDATE, because a big chassis is 5 000 rows and read-compare-write per MAC would be 10 000 round trips. NOT a history table: where a MAC was three weeks ago is something the SWITCH forgets in minutes |
| 112 | `snmp_credential_profiles` · `snmp_devices.credential_profile_id` | **one credential in many places, and SNMPv3.** Resolved ON THE SERVER — device override, the community it names, its site's, the global default — so the agent gets ONE credential per device and never tries alternatives: trying them in order is credential spraying, it locks v3 accounts, and on v2c a wrong community just times out. v3 keys live on a COMMUNITY, never a device row, and the security level is DERIVED from which keys are set (a stored level can disagree with the keys). Admin-only even to READ: a list of communities says which sites share a secret. See `docs/snmp-credentials.md` |
| 113 | `snmp_profile_locations` · `snmp_profile_agents` · `snmp_credential_profiles.is_global_default` | **a community is assigned to SITES and to AGENTS.** A site may hold SEVERAL, in its own `priority` order; an agent holds a GRANT, and **walks only with a community assigned to it** — one it is not granted is skipped as though unconfigured, and the device reports that it has no credential rather than polling with `public`. Several per site is an ORDER, not a retry list: the server sends the first one the polling agent may also use, and only that one. 112's single `location_id` is gone — assigned to no site and default for every site are opposite intentions, so the fallback is its own column. The migration backfills every existing community to every existing agent, so an upgrade changes nothing about what is polled. See `docs/snmp-credentials.md` |
| 115 | `change_acks` | **per-user acknowledgements on the Changes page** — `(user_id, ack_key)` → `acked_at DATETIME(3)`; `ack_key` is the row's `ackKey` (sha256 of the condition). A newer occurrence re-opens the row; 30-day life, pruned on the next ack. `usersRepository.listChangeAcks/ackChange/unackChange`. See `docs/changes-feed.md` |
| 116 | `device_counter_samples.duplex` · `.late_coll_pps` · `snmp_devices.sys_descr` | **duplex and late collisions become analysable** — duplex was validated and dropped; late collisions only existed as a raw counter. `duplex` is the agent's EtherLike `dot3StatsDuplexStatus` ('half'/'full'/'unknown', NULL = not answered); `late_coll_pps` is the per-interval rate. `src/devices/duplexMismatch.js` raises `if.<id>.duplex.mismatch` on half duplex with rising late collisions/FCS. `sys_descr` is the switch's own description (COALESCE, so an older agent never erases it) |
| 117 | `device_vlans` · `fdb_mac_moves` | **VLAN names per switch, and WHEN each MAC moved** — `move_count` (111) is all-time and the loop detector read it as a windowed count; `fdb_mac_moves` keeps one row per move (2 days, `RETENTION_FDB_MOVE_DAYS`) so `movingMacs()` counts moves inside the window. VLAN names age out with the forwarding table |
| 118 | `interface_state_transitions.device_id` · `.interface_id` · `.source` · `topology_changes.device_id` | **switch ports and switch-seen LLDP neighbours get a history** — reuses the 075 transition table (NULL `device_id` = an agent's own NIC) for switch-port link changes from polls and traps/syslog (`src/devices/switchPortStateService.js`: uplink down / flapping findings), and lets `topology_changes` carry SNMP-neighbour diffs (`topologyChangeService.processDeviceSnapshot`) |
| 119 | `flow_internal_rollup` | **long-term history for INTERNAL (LAN/OT) flows** — the external `flow_rollup` keys on peer country/ASN, which RFC1918 never has, so internal conversations used to vanish with their raw rows after `RETENTION_RAW_DAYS`. Hourly per (agent, src_ip, dst_ip, proto, service_port), the service port being the server end (`flows/services.servicePortOf`). Bounded: top `RETENTION_INTERNAL_ROLLUP_TOP_N` (500) per agent-hour, the rest folded into one `'*'` overflow row. Expires with the other rollups (90 d). See `docs/retention.md` |
| 120 | `idx_speedtest_ts` · `idx_txr_time` | indexes for the retention purges of `speedtest_results`/`transaction_results` (guarded, re-runnable). See `docs/retention.md` |
| 124 | `transaction_results.step_phases` · `transaction_tests.capture` · `transaction_captures` | **network-or-application evidence for transaction tests.** `step_phases` = the dns/tcp/tls/ttfb/transfer split of each step (free — read off socket events; `null` means the moment never happened, never 0). `capture` = `off`\|`on_fault`\|`always` per test. `transaction_captures` = the packet HEADERS of one run, keyed `(test_id, agent_id, time)` with no FKs so it matches its result row whichever frame lands first; `packets` is a bounded JSON field (≤2000, like `burst_runs`), and the verdict is stored beside the counts. Headers only — the agent's filter is derived from the test, snaplen is fixed at 96, and the ingest validator copies a fixed key list, so no payload/qname/SNI can reach the column. Retention **7 days**, the shortest in the product. See [docs/transaction-capture.md](docs/transaction-capture.md) |
| 121 | `probe_results.error_code` · `.failure` · `.resolver` | **why a dns/tcp probe failed** — resolver/socket errno (ENOTFOUND/ETIMEOUT/ESERVFAIL/…), the TCP probe's `refused`/`timeout`/`unreachable`/`error`, and the resolver asked. Read by `src/analysis/probeFailure.js` into the reachability finding (NXDOMAIN vs timeout vs SERVFAIL; RST vs silent drop; "ICMP ok, TCP/<port> blocked"). Per-hop `ips` (ECMP members) rides in the `hops` JSON, no column |
| 122 | `blueeye_nis2_incidents` Art. 23 fields | `suspected_malicious`, `cross_border_impact`/`_details`, `authority_reference`, `early_warning_/notification_/final_report_submitted_at` — the submission times turn a deadline into `submitted`. See `docs/nis2.md` |
| 123 | `blueeye_nis2_incidents.event_case_id` | the event case an incident was drafted from (`POST /api/nis2/incidents/from-event-case/:id`), FK `ON DELETE SET NULL` |
| 124 | `snmp_neighbors.protocol` · `.remote_address` · `.remote_platform` | **CDP beside LLDP** — `protocol` ('lldp'\|'cdp', default 'lldp') joins the unique key (`uq_snmp_neighbors_proto` replaces `uq_snmp_neighbors`), so a neighbour speaking both is two rows. `remote_address`/`remote_platform` are CDP's management IP + platform; the graph also resolves a neighbour by that address against a polled `host`. The topology ingest diffs neighbours per protocol. See `docs/snmp-topology.md` |
| 125 | `device_arp_entries` | **a polled router's ARP table** (IP-MIB, collect `arp`, ≤ 8192/device) — one row per (device, ip), `mac_changed_at` like `arp_entries`, same 30-day window (`RETENTION_ARP_DAYS`). Identity source for universal search and the new-device detector (per site via `snmp_devices.location_id`; `withDeviceArpDetection`). Repo `deviceArpEntriesRepository.js` |
| 126 | `snmp_devices.sys_location` · `.sys_contact` · `.sys_object_id` · `.hw_*` · `device_inventory` | **where a device is and what it is** — the SNMPv2-MIB system group (COALESCE on ingest; sysLocation shown as the room/rack beside the site in the switch list, search hits, coverage), the first chassis's ENTITY-MIB model/serial/vendor/revisions on the row, and every chassis + bounded modules in `device_inventory` (replaced per poll, searchable by serial via `snmpDevicesRepository.findBySerial`). New devices store `collect` = `DEFAULT_COLLECT` (adds `cdp`, `arp`, `entity`); a NULL `collect` keeps meaning the legacy four |
| 127 | `flow_records.vlan` · `.in_if` · `.out_if` | **which VLAN and which switch ports a flow crossed** — from the sFlow flow sample header (in/out ifIndex) and the 802.1Q tag (outer tag for QinQ; extended-switch record 1001 when the port stripped it); NetFlow v9/IPFIX IE 10/14/58/243. NULL from older agents. No index yet (nothing queries by VLAN; the heaviest insert path). See `docs/traffic-sources.md` |
| 128 | `sflow_exporters` | **every sFlow exporter an agent hears**, per (agent, address), matched to `snmp_devices.host` when the address is a registered device (`device_id`, SET NULL on delete). Matched exporters' counter samples feed `device_counter_samples` via `src/devices/sflowCounterIngest.js` (same delta/analysis path as SNMP counters); unmatched ones are a coverage gap (`sflowExporterUnregistered`) |
| 129 | `event_cases.cluster_id` | **a case knows its situation** — FK→`event_clusters` `ON DELETE SET NULL`, indexed. Stamped by the cluster sweep on the cases of a situation's member findings (first LIVE situation wins); shown as "part of situation #N" on the event page + Events list, the Situation page lists the cases; `autoResolveJob` does not resolve a case while its situation is open/acknowledged. See `docs/event-cases.md` |
| 130 | `event_clusters.grouping_basis` | **why a situation was grouped** — JSON `{ subjects, reasons, why }` from the target-aware correlator (shared target / same switch / upstream switch / same site / LLDP). Also lets a recurring fault on the same subject rejoin its open cluster after the old members aged out of the window. See `docs/cross-agent-correlation.md` |
| 131 | `known_devices` | **the new-device detector's long memory** — one row per (`scope` = `site:<id>` \| `agent:<id>`, MAC) with `first_seen`/`last_seen`/`last_ip`, so a device back after the 30-day ARP window is not "new" again. Seeded from `arp_entries`; touched after every ARP report; consulted before a MAC is called new; purged on `last_seen` after `RETENTION_KNOWN_DEVICE_DAYS` (400). Repo `knownDevicesRepository.js`. See `docs/discovery.md` |
| 132 | `probe_results.dhcp` | **what the DHCP test heard** (agent 0.40): `{ iface, timeoutMs, offers[], serverCount }` — every DHCPOFFER one broadcast DISCOVER drew (no REQUEST is ever sent, so no lease is taken). NULL = not measured (another type, or a test that could not bind port 68); an EMPTY `offers` list = nobody answered. `dhcp` joins DIAGNOSTIC_TYPES. See `docs/probe-dhcp.md` |
| 133 | `device_events.snmp_device_id`, `snmp_devices.sys_name`, `device_counter_samples.discontinuity` += `counter_reset` | **a switch's own events, its own name, a cleared counter** — `snmp_device_id` is the polled switch a syslog line/trap came from (resolved from the sender address against `snmp_devices.host`; `device_id` stays an AGENT id), indexed for the per-switch device log. `sys_name` is the SNMPv2-MIB sysName the agent sends on every topology poll (COALESCEd like `sys_descr`), matched by the topology graph, the L2 path and coverage against LLDP/CDP neighbour names, and searchable. `counter_reset` names why a rate is null when a counter went down with no reboot (`clear counters`). TSDB: `device_events.snmp_device_id` in `server/db/timescale/001_init.sql` |
| 134 | `change_mutes` | **per-user "Mute this rule" on the Changes page** — `(user_id, mute_key)` → `muted_until DATETIME(3)`; `mute_key` is the row's `muteKey` (sha256 of source + type, so every host). Always time-boxed (1..168h), only live rows read, expired pruned on the next mute. `usersRepository.listChangeMutes/muteChange/unmuteChange`. See `docs/changes-feed.md` |
| 094 | `service_monitors` · `service_monitor_results` | **Service Assurance monitors** — one row per non-browser check (mail, DNS record, RBL, LDAP bind, database, NTP, TLS on a port, TCP port) with its `config` JSON, its secrets as ONE AES-256-GCM blob, its own `interval_sec` and its current state (`last_status`, `consecutive_failures` — kept on the row so a sweep does not COUNT history per monitor per pass), plus a result row per check carrying the measurement, its unit and the per-phase `timings` that ARE the diagnosis. Also widens `service_test_incidents.subject_type` to `('test','certificate','monitor')`. See `docs/service-assurance-monitors.md` |
| 095 | `service_monitors.activated_at` | **the activation gate** — NULL means pending: saved, listed, checkable by hand, and NOT swept. The first check that comes back `ok`/`slow` stamps it, and while pending a failing check opens no incident and sends no alert (setting a monitor up is not an outage). `POST /monitors/:id/activate` is the operator's override, for when the service is down at the moment they start watching it. Existing monitors that had already run are backfilled, because an upgrade that silently paused an estate's monitoring would be the worst reading of the feature |
| 078 | `service_test_*` (15 tables) | **Service Tests** — applications · environments · credentials (AES-256-GCM) · allowed_hosts (the SSRF allowlist: host/ip/**cidr**) · tests (+steps +versions) · runs (**also the job queue**: `queued`→claimed by a worker with a conditional UPDATE) (+run_steps) · discoveries (+pages +elements) · suggestions · schedules · **settings** (every limit lives here, not in env). No FKs to existing BlueEye tables in either direction — the module is built to be extractable. `tenant_id` is present on the query roots but UNUSED in V1 |
| 080 | `service_test_certificates`, `service_test_incidents` | **Service Assurance reactions** (incidents also feed the Changes page — `listBetween()` + `fromServiceAssuranceIncidents()`, licence-gated at call time) — `certificates` is STATE (one upserted row per application+host+port: issuer, `valid_to`, `days_remaining`, `status` ok/expiring/expired/invalid/unreachable), read by a TLS handshake on its own schedule rather than only when a test trips over it. `incidents` is the durable "wrong since when": one row with `status='open'` per `subject_key` (`test:<id>` / `certificate:<application_id>:<host>:<port>`), opened when the condition holds, escalated when it worsens, resolved on the next healthy check. `notified_severity` is what was last SENT, so an alert goes out on a state change (opened / escalated / resolved) rather than once per observation. Thresholds live in the `assurance` settings section |
| 079 | `service_test_workers` | **Service Assurance worker heartbeats** — one upserted row per worker (`worker_id`, hostname, version, `started_at`, `last_seen_at`), written every poll tick. Liveness used to be inferred from the newest claim on the run queue, which called a freshly started idle worker "not connected" and told the operator to install what was already running. `GET /api/service-tests/runs/worker-status` reads it; a worker counts as gone after `queue.workerHeartbeatTimeoutMs` (60 s). Rows for workers unseen for a week are pruned by the queue's stale sweep |
| 077 | (renames) | **the incident vocabulary leaves the storage layer** — `incident_cases`→`event_cases`, `incident_notes`→`event_notes`, `incident_clusters`→`event_clusters`, `incident_playbook_runs`→`event_playbook_runs`, `findings.incident_case_id`→`event_case_id`; and, separately, the probe-derived `incidents`→`probe_outages` with `incident_thresholds`→`probe_thresholds`. Two different things had both been called "incident" and they split here. Every index and FK is renamed explicitly — MySQL carries the OLD constraint names through a table rename, so otherwise the schema would still be full of `fk_incident_*` pointing at `event_*`. **NOT renamed:** `blueeye_nis2_incidents` (the directive's own word, and regulators read the reports against it) and the ITSM subscription names `incident`/`anomaly` stored in customer integration configs (renaming them would silently unsubscribe live integrations). A **clean break**: no compatibility views, and the deprecated `/api/incidents/*` alias is removed with it. See `docs/events.md` |
| 076 | (columns) | **enrollment codes are no longer stored in cleartext** — adds `enrollment_codes.code_hash` (SHA-256, what every lookup matches on, UNIQUE) + `code_enc` (the code AES-256-GCM-encrypted via secretBox, decrypted only for the authenticated install-command endpoint), backfills the hash, then **drops the `code` column** and its index. A code is a credential — whoever holds one enrolls a machine as a trusted agent — so a database read should not yield usable ones, matching how `agent_tokens` has always been stored. The cleartext is dropped outright rather than kept for existing rows because nothing ever deletes an `enrollment_codes` row, so it would linger indefinitely. Pre-migration codes still **redeem** (their hash was backfilled); only re-displaying their install one-liner is lost (409 → generate a new code) |
| 075 | `interface_states` · `interface_state_transitions` | **interface up/down history** — the dimension the changes feed could not report, because interfaces are NOT a persisted entity (health is computed on the fly from `results.payload.traffic`). Recorded at the RESULTS-INGEST SEAM, the one place that sees every observation — never by polling current state, which would stamp events with the poll time and quietly lie about when things happened. Two tables mirroring the `lldp_neighbors`+`topology_changes` pair: a snapshot to diff against, and the history. A DISAPPEARED interface is deliberately not reported (far more often a collection hiccup than a real removal). A down VIRTUAL iface (docker0/veth/tun) is INFO, not CRIT. Flap collapse within `INTERFACE_FLAP_WINDOW_SECONDS` (300) folds a bouncing interface onto ONE row (`flapping 14×`) — and once flapping, every further transition collapses too, not only reversals. Retention `RETENTION_INTERFACE_TRANSITION_DAYS` default 90. See `docs/interface-transitions.md` |
| 074 | (column) | `users.last_seen_changes` — the per-user "changes seen up to here" marker behind the landing page. A COLUMN and not a `preferences` key on purpose: `preferences` is written by any settings change, while this must move only on an explicit mark-as-seen; sharing a write path is how a marker ends up silently advancing on every page load. NULL = never marked → falls back to the default window, not the epoch |
| 073 | `arp_entries` | **IP↔MAC identity source** behind the universal search field. The agent ALREADY collected `arp.table`, but only as an unparsed gzip blob in `cluster_evidence_snapshots` — this parses it into queryable rows. TWO ingest seams, both best-effort and both unable to break what they ride along with: `capabilities.arp` on the regular agent cycle (fresh, needs a new agent) and a harvest of the `arp.table` item during evidence capture (works on agents already in the field, fires only when a cluster opens); `source` records which. One row per (agent, ip) — a changed MAC is an UPDATE plus a `mac_changed_at` stamp, not history, so an identity lookup never becomes a time query. Scoped per agent (the same RFC1918 address exists at many sites). Age-out `RETENTION_ARP_DAYS` default 30. Pure parser `src/identity/arpTable.js` (four formats, detected per line). See `docs/arp-identity.md` |
| 072 | `event_notes` | **event work log** (shift handover) — append-only entries on an `event_case`, `kind` observation/action/**ruled_out**, author denormalised (`author_email`/`author_role`) so the trail survives user deletion (`author_user_id` ON DELETE SET NULL). Append-only is structural: no `updated_at`/soft-delete column, the repository exports no mutation method, and the router mounts only GET+POST. Every append is written to the hash-chained `audit_log` (the note TEXT is deliberately not copied there). See `docs/event-work-log.md` |
| 055 | `remediation_playbooks` · `event_playbook_runs` | **remediation playbooks** — pre-defined responses keyed to an anomaly-type (`trigger_condition` matched exactly against the event's primary finding metric; either `auto_trigger` or a `manual_action_text` runbook) + the event↔playbook run link (outcome recorded per event). Read by the event **recommendation** endpoint |

Interface health, traffic-type categories and **fleet health** add **no** tables — they
derive from the existing `results.payload.traffic` (and `flow_records.asn` for org
categories); fleet health is computed in `src/health/probeHealth.js` from `probe_results`.

## Dashboard (`public/app.js`)

A single vanilla-JS SPA. Key building blocks:
- **Routing** — `public/routes.js` is the one view ↔ path map, read by BOTH the
  dashboard and `src/app.js`. Every screen has an address (`/fleet`,
  `/probes/connection`, `/agents/12`); sub-tabs are path segments, filters stay
  in the query. `render()` is the only writer (it pushes history when the path
  changes), `popstate` the only reader. A browser navigation to an app path gets
  the shell (200); one inside the namespace that resolves to nothing gets the
  same shell with a 404 and `views.notFound`; a screen above the reader's role
  gets `views.forbidden`. See docs/ui-contract.md → Routing.
- **UI contract** — `public/css/tokens.css` is the one file colour lives in
  (palette + every `[data-theme]` block + the spacing/type/radius scale);
  `public/css/components.css` holds one implementation per component, scoped
  under the `ui` marker class. `public/uiPreview.js` is the phase-1 example
  screens on `/ui-preview/changes` and `/ui-preview/probes` (admin only, removed
  when those two screens migrate). See docs/ui-contract.md.
- **Layers** — every `z-index` comes from the `--z-*` scale in
  `public/css/tokens.css` (sticky → inline → topbar → sidebar → popover →
  rowmenu → scrim → modal → toast). A hand-picked number is a `ui:check`
  finding; `test/layerScale.test.js` pins the order. Leaflet's own panes (400+)
  are clamped by `.leaflet-container { isolation: isolate }` — without it the map
  paints over the drawer, the modal and the toast.
- `el(tag, attrs, ...kids)` — DOM helper. `api(path, opts)` — fetch + bearer + 401 handling.
- `dataCard(title, { actions, note }, ...body)` — the framed page section every
  view's data lives in (heading + actions on top, table flush to the card's
  edges). Page width is one rule for all views: `main#view` is `width: 100%`
  capped at `--page-max`. See docs/design.md.
- `views.<tab>` — async function per tab returning a node (`changes` (**the landing
  route** — what happened since you last looked; see docs/changes-feed.md),
  `fleet` (UI label **“Fleet”**, no longer the landing route — still the right
  screen for bulk operations across agents; compact `networkPath` strip + a fleet-wide **traffic
  map** (`trafficMapCard`, colored directional flow arrows; arc click → Flows→Map
  scoped to the site, pin click → the location page) and ends with a gated
  **“Open issues”** rollup (`fleetIssues()`, events + findings,
  `dashboard_advanced`) for Professional+),
  `overview`, `map` (UI label **“Sites”** — locations coloured by agent health),
  `geo` (UI label **“Destinations”** — external traffic by country/ASN),
  `agents`, `interfaces`, `nics` (NIC firmware inventory + drift), `probes` (four sub-tabs:
  **Run a probe** · **Connection test** — one address, the whole battery, with rounds/Stop/Repeat ·
  **Burst mode** — one target once a second while the fault is happening · **Test packages**), `flows` (Unified / Bidirectional / **Map** — geographic traffic arrows via `/api/flows/map`), `screening` (**Test area** — admin-only outbound screening), `findings`, `locations`, `enrollment`,
  `docs` (**Documentation** — built-in handbook: getting-started + troubleshooting how-tos for everyone, admin-only setup guides; static, RBAC-gated content),
  `snmpDevice` (**a polled switch** — reached from the device list, a port search hit or a loop
  finding, no tab: a switch has no token, heartbeat or version and does not belong in the rail beside the agents),
  `about` (**About** — reached from the account menu, not the rail: this host's version +
  release date from `/system/version`, then the dated feature history from `public/about.js`),
  `settings`) plus `agent` (the combined per-agent drill-down page, no tab —
  reached via `openAgent(id)`) and `location` (the per-site drill-down —
  agents + scoped health + data flows, reached via `openLocation(id)`).
  All maps init via the shared `createLeafletMap`
  (server-configured EU/self-hosted tiles).
- `render()` — mounts the current view + its `hero()`; stops per-view pollers
  (`stopOverview`/`stopProbes`/`stopIfaces`/`stopFleet`/`stopAgent`/`stopGeo`) when leaving.
- Shared renderers `interfaceTable()` / `probeLatestTable()` / `probeDetail()` back both
  the standalone tabs and the combined agent page. `probeLatestTable()` carries ONE
  adaptive **Measured** column (`probeMeasured()` — latency+loss for a ping, hop count
  for a trace, packet size + recommended MSS for a path-MTU check) rather than the fixed
  RTT/Loss/Jitter triple that fitted ping and left four of the nine types blank. A row
  opens its detail IN PLACE, using the same disclosure mechanics as the Service Assurance
  monitor results table (`sa-result-row` / `sa-trace-row`): one open at a time, because two
  open traces would each mount a `pathVisualization` and those persist the brush window to
  the URL. The detail is fetched once per row and the 5 s refresh pauses while a row is
  open — replacing the tbody would close it — and the pause is stated, not silent.
  `probeDetail()` stays callable standalone for the topology "Show route" modal.
- `PAGE_INFO` — per-page hero line + "Mere info" drawer text.
- Charts are hand-rolled SVG: `multiChart` (live, area + time ticks + brush) and
  `historyChart` (time-axis; optional `band` = robust normal-range shading via
  `robustBand`, `markers` = event lines via `findingMarkers`). `usageBar()` for utilisation bars.
- Theme: a catalogue of colour palettes (`PALETTES`: default/midnight/nord/forest/sunset/
  solarized/contrast), each in a **light + dark** variant. Pick a palette in **Settings →
  Appearance**; the **account menu**'s "Dark mode" row (topbar `#user-menu`, `#theme`)
  flips brightness within it (`dual`). **Saved per user** (`/me/preferences`), cached in
  localStorage for instant apply. Mobile: tab bar → bottom nav.

- Topbar account menu (`#user-menu` in `index.html`): one dropdown, opened from a trigger
  showing the signed-in user (`#whoami` = email + role badge) + a chevron. Houses **Refresh**
  (full `location.reload()`) with the **auto-refresh** toggle (`#autorefresh`; persisted in
  `localStorage` under `AUTOREFRESH_KEY`, restored + started on load, reload on change),
  **Dark mode** (`#theme`), **About** (`#about`, `data-view="about"`) and **Log out**
  (`#logout`). Closes on outside-click + Escape — and on an item that navigates, via
  `closeUserMenu()`, since the menu now holds a nav entry. Everything that navigates is
  wired through the one `NAV_BUTTONS` selector in `app.js` (the rail, its foot, this menu).

## Where do I change…?

| Task | Start here |
| --- | --- |
| A new HTTP endpoint | `src/routes/<x>.js` + mount in `routes/index.js` + a fake in `test-support/fakes.js` |
| "Put the new feature on the About page" | One entry at the TOP of `RELEASES` in `public/about.js`: `{ v, d, area, en:{t,s}, da:{t,s} }` — the version it ships in, the date it lands, and both languages (the history is data, not catalogue keys; only the page chrome is `about.*` in `public/i18n.js`). `test/about.test.js` enforces the order, the areas, both languages and that nothing claims a version newer than `package.json` |
| A DB table/column | new `migrations/NNN_*.sql` + repository in `src/repositories/` |
| "This event should be a warning for us, not a critical" | Severity rules. The judgement is pure in `src/events/severityRules.js` (most specific rule wins; a tie goes to the newest); data + the 30s cache the write path uses in `src/repositories/severityRulesRepository.js`; HTTP in `src/routes/severityRules.js`. Applied where events are STORED — `FindingStore.save()` (`src/analysis/findings.js`) and `src/serviceTests/assurance/reactor.js` (through the `severityRules` **port**, since nothing under `src/serviceTests/` may require a host module). UI: Settings → Severity rules + a "Severity rule…" button on each finding and incident. `docs/severity-rules.md` |
| "Discover what is BEHIND the login" | `src/serviceTests/discovery/authenticate.js` (pure) decides what can sign in and whether the session still holds; the worker\'s `signIn()` drives it in the SAME browser the crawl uses. Two routes: replay a login test, or fill in the login form the anonymous pass found and stored as `detected_login` (migration 091) — the second is what makes the FIRST authenticated discovery possible. A lost session stops the crawl (`STOP.SESSION_LOST`) rather than mapping the public site and reporting it as private |
| "What is the incident, and what happened when?" | `src/serviceTests/incidents/lifecycle.js` — states (open/investigating/identified/resolved/closed, with backward paths so "we were wrong" does not need a close-and-reopen), the timeline built from real events with real timestamps, impact from the worst affected journey, and `INC-YYYY-NNNNN` derived from the id rather than a collision-prone counter. Migration 090 widens the V2 incident and adds `service_incident_events`. **`ACTIVE` is what "not resolved" means** — six repository queries ask for it by name, because `status = 'open'` would hide an incident somebody picked up |
| "Why did this fail, and how sure are we?" | The correlation engine, `src/serviceTests/correlate/correlate.js`. Relates observations into a chain (symptom → technical cause → what was ruled out) and names a LAYER with a confidence built from what was RULED OUT — every layer nobody looked at lowers it, by name. Inferred clearances ("the network must be working, because other requests answered") are labelled as inferences, never as probes. Capped at 92: a rule-based inference is never a fact. A passing run correlates to null. Ranking specific causes within a layer is Phase 2 root-cause analysis, which reads this |
| "What did this run actually observe?" | V3 observations. `src/serviceTests/observe/observations.js` turns a completed run into typed facts (layer + kind + outcome + evidence), stored in `service_observations` (migration 089). `unknown` never collapses into `ok`; a request that never completed is a network fact, not an API one. `layerSummary()` is what makes "Network ok, Server ok, API failed" sayable. `docs/service-assurance-v3.md` |
| "Is this service healthy, and why?" | `src/serviceTests/health/serviceHealth.js` — one assessment from journey verdicts weighted by criticality, layer observations, performance against baseline and repetition. A critical journey failing fails the service; slow is never FAILED; UNKNOWN is a real state. The 0-100 score is published in four parts with its weights, and an unknown part is left out rather than scored zero |
| "Did the page change how it LOOKS?" | Visual regression (V2 §8). Pure PNG decode in `src/serviceTests/visual/png.js` (zlib + five filters, no dependency), the pure comparison in `visual/compare.js` (colour tolerance, ignore regions, percentage threshold), per-step capture by `captureFor()` in `runner/execute.js`, baselines on disk at `<artifacts>/baselines/<test>/` (NOT in a run directory — retention would delete them), rows in `service_test_baselines` (migration 088), HTTP in `api/baselines.js`. **A difference is a warning, never a failure**, and accepting a baseline is an explicit act that records who and when. `docs/service-assurance-visual.md` |
| "Is this page usable by everybody?" | Accessibility checks (V2 §9). Pure rules in `src/serviceTests/a11y/rules.js`, the read-only in-page collector in `a11y/collect.js`, attached to the run by `auditPage()` in `runner/execute.js`, stored in `service_test_runs.accessibility` (migration 087). **A finding can never fail a run** — reported beside the result, in its own panel with none of the failure vocabulary. Off switch: `runner.accessibility`. `docs/service-assurance-accessibility.md` |
| "Add a check that is not a browser (mail delivery, SPF/DMARC, a blacklist, an LDAP bind, a clock, a certificate on port 993)" | A **monitor**: one entry in `src/serviceTests/monitors/types.js` (fields + bounds + which are secrets) and one checker in `monitors/checks/`. Everything else is derived — the validator (`validation/monitors.js`), `GET /api/service-tests/monitors/types`, the UI form (`views.monitors` in `public/serviceAssurance.js`) and the registry. Verdict → severity is `monitors/policy.js`; the sweep is `sweepMonitors`/`createMonitorsJob` in `assurance/reactor.js`. `docs/service-assurance-monitors.md` |
| "Run a journey / rename or re-rate one" | `POST /api/service-tests/journeys/:id/run` queues one run per member test, in order, using the journey's environment (`src/serviceTests/api/journeys.js`). `PUT /journeys/:id` edits name, description, criticality and expected duration; the dashboard's `journeyForm` / `runJourney` are in `public/serviceAssurance.js`. `docs/service-assurance-journeys.md` |
| "Discovery suggests a journey I already have" | `acceptJourney` in `src/serviceTests/api/discovery.js` answers **409** with what each existing journey already covers and what would be added, scoped to the application and matched by test id or name. `{merge_into_journey_id}` appends the missing steps to that journey (reusing its same-named test rather than creating a second); `{confirm:true}` creates a separate one. Nothing is built while the question is open |
| "Rename / delete a service test" | `PUT`/`DELETE /api/service-tests/tests/:id` (`src/serviceTests/api/tests.js`). The designer only ever PUTs `{ definition }`; name, description, credential and enabled come from the Edit dialog (`testForm` in `public/serviceAssurance.js`). Deleting cascades the test out of any journey that used it, which the confirm names before it happens |
| "Is a newer server/agent published?" | The versions arrive **inside the signed licence proof** — `licenseManager.getAvailableReleases()` (`src/license/licenseManager.js`), compared with `src/lib/version.js` and surfaced on `GET /system/version` (`upstream`). UI: Settings → Updates (`upstreamUpdateSection` in `public/app.js`). Deploying it: `src/services/serverUpdateService.js` + `POST /system/server-update`, opt-in via `SERVER_UPDATE_COMMAND`. See `docs/updates.md`; the publishing side is blueeye-licens `docs/release-tracking.md` |
| Upgrading a deployed agent | Push-update (systemd only): `POST /agents/:id/update` in `src/routes/agents.js` (signed release preferred, source bundle fallback) → the agent's `update` handler. **Windows agents cannot be pushed to** — they run under a Scheduled Task, so the dashboard's "Update" button shows the update-in-place command instead: `GET /api/enroll/update-command` (`src/routes/enrollCommand.js`) → `GET /enroll/update.ps1` (`renderUpdatePs1` in `src/enroll/installScriptWin.js`); UI `showWindowsUpdateCommand`/`agentUpdateButton` in `public/app.js`. **An agent that refuses the update** ("refusing unsigned update" / "signature did not verify") has pinned a release key this server no longer signs with: `POST /agents/:id/rekey` (`src/routes/agents.js`) sends this server's current release key over the agent channel, together with the **vendor-signed licence proof** that authorises its fingerprint (`licenseManager.getTrustProof()`; the agent verifies it against a vendor key it embeds, so a compromised server cannot re-anchor its own fleet — see `docs/updates.md`) and the agent replaces its own anchor (no shell on the host); the dashboard's `showRepinCommand`/`bulkRepinAgents` in `public/app.js` drive it, and `GET /api/enroll/repin-command` → `GET /enroll/repin.sh` is the host-side fallback for an agent that is offline. That script carries no enrollment code and refuses a host with no enrolled agent, so it never creates a second agent. See `docs/enrollment.md` + `docs/updates.md` |
| The Windows install/update command (and why it is not `irm … \| iex`) | `winRunSteps` in `src/enroll/installScriptWin.js` builds it — download the script to a file, then run the file. The cradle form trips an IPS rule on the wire (*ET ATTACK_RESPONSE PowerShell NoProfile Command Received In Powershell Stagers*) and endpoint AV on the host. Consumed by `src/routes/enrollCommand.js` (`oneLiner` + split `steps`) and echoed inside the generated scripts. See **Why not `irm … \| iex`** in `docs/enrollment.md` |
| "A user typed a symptom — what do we tell them to do?" | `src/diagnose/`. The knowledge is DATA (`playbooks/*.json`, one file per fault, every string `{en,da}`) so **a new playbook is a new file, not a code change** — it is validated at boot and matchable immediately. `match.js` ranks it without any AI (Danish + English, stemmed, generic words discounted); `llm.js` is the optional accelerator and may only CHOOSE from the catalogue. `expr.js` is the rule language (no `eval`, three-valued so a test that has not run decides nothing); `facts.js` turns probe results into what rules read and owns `FACT_SCHEMA`, which the catalogue validates every rule path against. `evaluate.js` decides the verdicts — **in code, never by a model**. HTTP in `src/routes/diagnose.js`, screen in `views.diagnose`. `docs/diagnose.md` |
| "Add a fault to the diagnosis catalogue" | One JSON file in `src/diagnose/playbooks/`, named after its id. No code. The loader refuses it unless both locales are complete, every test type is a real probe, every view is a real screen, every rule path and fix placeholder is in `FACT_SCHEMA`, and at least one rule can confirm it — a malformed playbook **stops the server**, because loading happens at boot rather than on the day somebody needs an answer. Add its confirming / ruling-out / inconclusive patterns to `test/diagnoseRules.test.js`, which asserts every shipped playbook has all three |
| "Does this path carry a full-size packet, and does it SAY it does not?" | The `path_mtu` probe and its storage are `docs/path-mtu.md` (`probe_results.mtu`, mig 096, per-hop in the ordinary `hops` column). The **ping size sweep** beside it is `probe_results.sizes` (mig 097) — the same target at several payload sizes with DF set, whose TOP-LEVEL metrics are the SMALLEST size so a blocked 1472 never reads as an outage. `src/diagnose/facts.js` turns both into what the diagnosis rules read, and derives `mss_exceeds_path`: a negotiated MSS above the measured path is the direct evidence that clamping is missing, left ABSENT rather than false when the agent could not read it (`mssSupported:false` is "could not look", not "nothing to report"). `path_mtu` is excluded from uptime and fleet health (`DIAGNOSTIC_TYPES`) — the probe reports `ok:true` even on a blackhole, so nothing else stops an investigation moving the SLA number it is investigating |
| "Why is this screen empty?" | **Settings → Setup** — one screen for the whole product, because it was one question asked six times. `GET /api/setup/checklist` (admin) over `src/services/setupChecklist.js`: agents enrolled, a traffic source that carries flows (proc does not — and an sFlow exporter on the HOST does nothing until the agent is told to collect it), switches to poll, a credential each switch may actually use (asked of `resolveForAgent`, because "a community exists", "its site has it" and "this agent is granted it" are three conditions), a site on each switch, syslog/traps arriving, GeoIP loaded, coordinates on the sites. EVERY ROW IS COMPUTED, never ticked: a checklist somebody ticks starts lying the moment the configuration changes. A source that cannot be read is `unknown`, never zero, and an `unknown` never holds the list open — the Device log can never be PROVEN set up from here (the agent's receivers are local config), and a checklist that can never finish is one people ignore. UI `settingsSetupView` in `public/app.js`, strings under `setup.*` |
| "Which SNMP community does this switch get polled with, and who may use it?" | Settings → SNMP communities. A named credential (`snmp_credential_profiles`) assigned to SITES (`snmp_profile_locations`, ordered per site) and to AGENTS (`snmp_profile_agents`, the grant). Resolution is `snmpCredentialProfilesRepository.resolveForAgent()` — the device's own community, the one it names, its site's list in order, the global default — every named step filtered by what the polling agent is granted, and the FIRST survivor is the only one sent. An agent walks only with a community assigned to it; a target with none is refused by the agent (`snmpPoller.credentialError`, `openSession` → `SNMP_NO_CREDENTIAL`) and never falls back to `public`. Routes `src/routes/snmpProfiles.js` (admin, incl. `PUT /order/:locationId` for the site's order); UI `settingsSnmpCommunitiesView` in `public/app.js`, strings under `snmpcom.*`. See `docs/snmp-credentials.md` |
| "Is this port running half duplex while its partner runs full?" | Late collisions. Walked off the EtherLike-MIB (`dot3StatsLateCollisions`, `1.3.6.1.2.1.10.7.2.1.8`) by blueeye-agent `src/snmpMonitor.js`, surfaced as `lateCollPerSec` by `src/health/interfaceHealth.js`, read as `iface.late_coll_per_sec` by `src/diagnose/facts.js`, and confirmed outright by the `late_collisions` rule in the `duplex_mismatch` playbook. **`null`, never 0, when nothing could count them** — a /proc sample never can and many switches omit the MIB, and since a measured zero is what RULES THE FAULT OUT, the absence has to stay distinguishable the whole way. The interface layer is strict where it coerces its other counters, because `Number([])` is 0 |
| Anomaly thresholds / detection | `src/analysis/detector.js`, `config.js` (editable via Settings→Analysis) |
| Alert channels | `src/analysis/alerting/channels/*` + `dispatcher.js`; runtime-editable via Settings→Alerting (`settingsService.getAlerting/setAlerting`, `PUT /api/settings/alerting`, UI `settingsAlertingView` in `public/app.js`) — live-applied onto the running `alertingConfig`, secrets write-only |
| Maintenance windows / silencing | `src/analysis/alerting/maintenance.js` (`createSilencer`) + dispatcher hook; windows in `settingsService` (`maintenance` key), route `/api/settings/maintenance` |
| Data retention | `src/analysis/retention/*` (editable via Settings→Retention) |
| Storage backends (MySQL + optional TimescaleDB) status | `settingsService.getTsdb()` (read-only, env-driven — the pg pool is boot-time infra, no `setTsdb`) exposed on `GET /api/settings`; live sizes on `GET /system/storage` (`services/systemInfo.js`). **UI: Settings → Database** (`settingsDatabaseView`) with status + how-to. Configure via `TSDB_*` env (`src/config.js`) + `deploy/install-timescale.sh`; see `docs/storage-split-audit.md` |
| Geo/ASN enrichment | `src/geo/enricher.js`, `provider.js`; flows in `flowsRepository.js` |
| Where a traceroute hop lands on the map | `src/geo/hopLocation.js` (`locateHop`: router PTR name → city GeoIP → country centroid, each checked against the fastest RTT — ~100 km per ms; an impossible placement goes to `geoRejected`), `hostnameHints.js` + the curated code table `networkPlaces.js` (add a city/code there), `cityProvider.js` (DB-IP City Lite in typed arrays; built by `geoipBuild.buildCityFromSource`, the updater and `scripts/build-geoip.js --city`). Hostnames come from agent 0.40+ (`hop.hostname`). See `docs/geo.md` → "Traceroute hops" |
| Traffic-type categories | `src/flows/categories.js` (editable via Settings→Traffic types) |
| Flow/conversation explorer | `flowsRepository.exploreFlows` + `src/routes/flows.js` (`/explore`); UI `views.flows` |
| Traffic map (colored flow arrows) | `flowsRepository.mapFlows` + `src/routes/flows.js` (`/map`, classifies via `src/flows/categories.js`, places via `src/geo/centroids.js`); UI shared helpers `drawTrafficMap`/`trafficMapCard`/`trafficLegendChips` in `public/app.js` — used by Flows→Map mode, the Overview (below the compact `networkPath`) and `views.location` |
| Location drill-down page | `views.location` in `public/app.js` (`openLocation(id)`, no tab) — scoped KPI cards + agents table + traffic map/dataflows; entry points: agent-page site name, Locations list, traffic-map site pins |
| Active probes (server) | `src/routes/probes.js`, `probeResultsRepository.js`, `validation/probeValidation.js` (probe types incl. `http` + `curl` + `pageload` + multi-step `transaction` + `path_mtu` + `tls` + `rdns`) — agent side in blueeye-agent `src/probes/` |
| Connection test (one address, every check) | The catalogue is `src/connectionTest/checks.js` — the ONLY list, served by `GET /api/connection-test/checks` and used to build the probe specs a run dispatches, so a check the screen offers is always one the server can send. Router `src/routes/connectionTest.js`, input validation `src/validation/connectionTestValidation.js` (the target goes through `validateProbeSpec`, never a second host pattern). Recurrence maths + validation: `src/schedule/recurrence.js` (pure); storage `test_packages.schedule_spec` (mig 099); ticked by `src/services/testPackageScheduler.js`. UI: `connectionTestView()` + `openRepeatModal()` in `public/app.js` (`ct.*` keys in `public/i18n.js`, en+da). **Adding a check is one entry in checks.js** — plus its `ct.check.<id>` / `ct.check.<id>.desc` keys in both catalogues. See `docs/connection-test.md` |
| Burst mode (1 Hz, on demand) | Analysis `src/probes/burstAnalysis.js` (PURE — median + MAD from `src/analysis/baselines.js`, never a second definition of “typical”); dispatch + recording `src/probes/burstService.js` (the run id IS the command id, so a result always matches its run); route `src/routes/burst.js`, validation `src/validation/burstValidation.js` (REFUSES out of range where the agent CLAMPS — both, because the thing that emits the packets must not trust the thing that asked); storage `burstRunsRepository.js` (mig 107). Live samples ride `burst_sample` → `burst-sample` through `src/ws/agentSocket.js`. UI `burstView()` + `burstChart()` in `public/app.js` (`burst.*`, en+da). Agent `blueeye-agent/src/burst.js`. See `docs/burst-mode.md` |
| Live traceroute on the Destinations map | Agent streams `trace_hop` per hop (`blueeye-agent/src/probes/traceroute.js` `streamHops`, sent from `runProbeAndSubmit` in `runtime.js`); `src/ws/agentSocket.js` (`traceHopPayload`) geolocates it with `describeLiveHop` (`src/analysis/pathGraph.js`) and relays `trace-hop`; `POST /agents/probe-results` sends `probe-result` when a trace lands. UI: `runTrace`/`waitForPath`/`drawGeoPath` (one layer per trace in `geoState.traces`) in `public/app.js`, trace list + live hop list in `public/views/destinations.js` (`dest.path.*`). See `docs/path-visualization.md` |
| Interface counters (per switch port) | The arithmetic is `src/devices/counterDelta.js` — PURE, and the only place that decides when a delta must NOT be computed (reboot via sysUpTime against ELAPSED REAL TIME, an ifIndex renumbering, a gap, a 32-bit wrap). Ingest `src/devices/snmpCounterIngest.js` (resolves a sample to a port ROW by name, reads the previous sample per device in one query). Storage `deviceCounterSamplesRepository.js` + the TSDB twin, migration 109. Agent `blueeye-agent/src/snmp/counters.js` (one read per cycle, raw values) and `snmpPoller.runCounterCycle` (own interval, four devices at a time — sequential cannot fit twenty switches into a minute). sFlow counter samples feed the same series through `src/devices/sflowCounterIngest.js` (registered exporters only; SNMP-polled devices are left to the poll; exporters heard go to `sflow_exporters`, migration 128, for the coverage gap). See `docs/device-counters.md`, `docs/traffic-sources.md` |
| Switch-port link history, rule-based switch findings | `src/devices/switchPortStateService.js` — polled status changes (`deviceInterfacesRepository.upsertMany` → `statusChanges`) and `link.*` traps/syslog (via `deviceEventIngest`, matched by sender address + port name) into `interface_state_transitions` with `device_id` (migration 118), flap collapse, findings for an uplink down / a flapping port. `src/devices/duplexMismatch.js` — half duplex + late collisions/FCS. `src/devices/findingSink.js` — the ONE path (store, publish, event case, alert, integrations) every rule-based switch finding takes, the L2 loop included. Switch-seen LLDP diff: `topologyChangeService.processDeviceSnapshot`. VLAN names + MAC-move history: `fdbEntriesRepository` (migration 117). See `docs/changes-feed.md`, `docs/l2-loop.md` |
| Findings on a switch port | `src/analysis/deviceIngest.js` turns a stored counter row into MetricSamples — RATES only (a raw counter is monotonic, so its median is meaningless and every sample is an all-time high), and nothing at all from a row whose rates were voided. The port id is IN the metric name (`if.12.in.errPps`) because baselines key on (hostId, metric, bucket) and one flapping port must not raise the baseline for forty-eight. `pipeline.processDeviceSamples()` runs the SAME detector, event grouping, correlation and alerting as the agent path. Filter with `GET /api/findings?deviceId=&interfaceId=` |
| L2 loop detection | `src/analysis/l2Loop.js` is PURE — three facts in, a verdict out: MACs flapping between two ports (the signature), broadcast surging on many ports at once, STP reconverging. Each is ORDINARY alone (a laptop being moved, a backup starting, a port coming up), so the first carries the case and the others only corroborate. `l2LoopService.js` gathers them from what is already stored and raises a finding whose explanation NAMES THE TWO CABLES. Runs from the topology ingest, when the forwarding table has just been re-read. See `docs/l2-loop.md` |
| A switch's own page | `public/views/snmpDevice.js` (RecordPage) — the first screen here that IS a piece of network equipment. The port table is keyed on the NAME, a port that cannot answer says so rather than showing 0, and a row OPENS its chart under the table (forty-eight sparklines is a wall nobody reads). Errors get their own chart, because plotted against bits per second they are a flat line at the bottom of the axis — which is the one thing somebody opened the page for. Reached from the device list, a `port` search hit or a loop finding; `openSnmpDevice(id)` in app.js |
| DHCP test (no answer / rogue server) · host NIC duplex | Agent `blueeye-agent/src/probes/dhcp.js` (pure RFC 2131 codec + injectable dgram runner) and the proc source's duplex/frame/fifo/colls/carrier fields (`blueeye-agent/src/trafficMonitor.js`). Server: `dhcpBlock` + the `dhcp` spec branch in `validation/probeValidation.js`, `probe_results.dhcp` (mig 134), findings `probe.dhcp.no_offer` / `probe.dhcp.rogue` in `analysis/probeFindings.js` (`dhcpFindings`, sentences from `analysis/probeFailure.js` `describeDhcp`), UI `dhcpDetail()` in `public/app.js`. Duplex: `reasons` in `health/interfaceHealth.js` (`reasonsOf`), the fleet reason in `health/probeHealth.js`, the `iface.duplex`/`collisions_per_sec`/`frame_err_per_sec`/`carrier_err_per_sec` diagnose facts. See `docs/probe-dhcp.md` |
| TLS certificate / reverse DNS | Agent-side `blueeye-agent/src/probes/tls.js` + `rdns.js` (pure node `tls`/`dns` — no shell, no raw socket, so they run anywhere the agent does). Server: spec + result validation in `validation/probeValidation.js` (`tlsBlock`/`rdnsBlock`), storage `probe_results.tls`/`.rdns` (mig 101), findings in `analysis/probeFindings.js` (`certFindings` — the expiry countdown reads BOTH `http` and `tls`, and an untrusted/mismatched/expired certificate is a separate CRIT), UI `tlsDetail()`/`rdnsDetail()` in `public/app.js`. `rejectUnauthorized:false` on the handshake is deliberate: a refused handshake cannot say WHICH fault it was. See `docs/probe-tls-rdns.md` |
| Repeat something on a schedule (any screen) | One dialog, five screens: `recurrenceFields()` + `openRepeatModal()` in `public/app.js` (keys `repeat.*`, en+da). The Connection test, a single probe, a speed test, a diagnosis plan and the test-package editor all write the SAME `schedule_spec`; only the payload differs, so the caller owns the saving (`saveRepeatPackage()` for the per-agent ones). Adding Repeat to a new screen is a dialog call plus whatever it saves — never a second schedule model |
| Add a tab strip to a screen | `tabStrip(items, { active, onPick, ariaLabel })` in `public/app.js` — the ONLY way to build one (a gate rule refuses a hand-rolled `.subtabs`). It carries the look (`.subtabs`/`.subtab` in `styles.css`: no button chrome, the accent underline on the strip's own rule) AND the behaviour a stylesheet cannot give it — roving tabindex so the strip is one stop in the tab order, arrow/Home/End inside it, `role=tablist`/`role=tab`/`aria-selected`. Focus moves without switching: each tab here renders a screen and calls the API, so arrowing past one must not load it. **A grouped rail of separate screens (Settings, Documentation) is NOT a tab strip** — that is `.navlist`, which keeps the quiet-chip look on purpose. Service Assurance repeats the strip in its own files by design (the module is extractable); the gate checks it there rather than exempting it |
| Scheduled reports (mailed, recurring) | `src/routes/reportSchedules.js` + `src/services/reportScheduler.js` + `src/services/reportMailer.js`, over `report_schedules` (mig 100). The report itself is `src/reports/definitions.js` — **add a report there and both the export and the scheduled send get it**. Screen: `reportSchedulesPanel()` in `public/app.js` (Reporting → Scheduled reports). See `docs/scheduled-reports.md` |
| Path MTU (packet-size verdict) | Agent probe `blueeye-agent/src/probes/pathmtu.js` (IPv4 + IPv6, per hop; family knowledge in `blueeye-agent/src/probes/ipFamily.js`); server validation + wire→storage translation in `validation/probeValidation.js` (`mtuBlock`, the `path_mtu` spec branch), stored in `probe_results.mtu` JSON (migration 096) with per-hop `maxMtu`/`status` riding in the existing `hops` column. Root cause: `src/analysis/mtuFindings.js` (`probe.mtu.blackhole` CRIT / `probe.mtu.reduced` INFO / `probe.mtu.clamp` WARN), called from `probeFindings.js`. UI: `mtuDetail()` in `public/app.js` + the "Test MTU on this path" shortcut on `pathVisualization`. Starting one is written to the hash-chained `audit_log` as `agent`/`probe_start`. See `docs/path-mtu.md` |
| Path visualization (traceroute map) | `src/analysis/pathGraph.js` (`buildPathGraph` — aggregates traceroutes → hop graph, geo/ASN + city placement via `src/geo/hopLocation.js`, severity; **`branches`** = additive multi-run ECMP inference + `worstHopIndex`), `GET /api/probes/path`; UI `pathGraph()` in `public/app.js` (traceroute detail) — SVG topology **+ a geographic Leaflet map** (`drawPathMap`/`pathGeoStops`/`renderPathStops`, source anchored at the agent site via `agentsRepo` `location_lat/lng`); also a **path picker in `views.geo`/Destinations** (`drawGeoPath`). Per-hop loss/jitter come from MTR-style agent traceroute. See `docs/path-visualization.md` |
| Path Visualization shared component (graph + metric timeline) | `pathVisualization(opts)` in `public/app.js` — one reusable component mounted in **Probes** (`probeDetail`), **Topology** (`actionBtns` drawer), **Tests** (`txDetailView`) and **Troubleshooting** (`views.event` "Affected path"). Path graph (extended `pathGraph()` with ECMP branches, problem-hop ring, click→timeline) + a brushable metric timeline (`pathVizTimeline`/`pvChart`: overview strip + detail, metric selector, per-agent overlay, opt-in "Explain selection" → `/api/assistant/explain`), URL-persisted (`?from&to&metric&overlay`). Backend: pure DST-safe bucketing `src/analysis/pathTimeseries.js` (`METRICS`/`bucketMetric`) + `probeResultsRepository.metricRows`; `GET /api/probes/path/timeseries` + `GET /api/probes/path/metrics` (viewer+). See `docs/path-visualization.md` |
| AS-path view + change detection (BGP-flavoured) | `src/analysis/asPath.js` (`extractAsPath`/`diffAsPath` = observed forwarding AS-path + change classification; `asGraphFromNodes` = AS-level projection of the hop graph). API: `asGraph` block on `GET /api/probes/path` (`routes/probes.js`). UI: **Hop view / AS view** toggle in `pathGraph()` (`public/app.js`). Findings: `asPathFindings` in `src/analysis/probeFindings.js`, fed a `geoProvider` via `probePipeline.js` (wired in `src/server.js`) → reroute/origin-AS-change findings (`probe.aspath`) through the normal store/alerting pipeline. Data-plane only (not BGP `AS_PATH`). See `docs/path-visualization.md` |
| Per-target event timeline (merged view) | **Backend:** `src/routes/targets.js` (`GET /api/targets/:id/timeline`, viewer+) + pure merge `src/timeline/targetTimeline.js` (`buildTargetTimeline`, reusable) + fan-out/partial `src/timeline/targetTimelineService.js`; reads `findings` + probe-outage `events` (`probeOutagesRepository.listForAgent`) + agent connect/disconnect (`auditEventsRepository.findByActor`) + playbook runs (`remediationPlaybooksRepository.listRunsForHost`). Read-only, no schema changes. **Frontend:** `targetTimelineCard()`/`timelineRowEl()` on the agent detail page in `public/app.js`, backed by the pure, unit-tested `public/timelineView.js` (`window.TimelineView`; range presets + state resolution + row model — loaded via its own `<script>` in `index.html`). See `docs/target-timeline.md` |
| Event derivation (open/resolve) | `src/probeOutages/detection.js` (pure: threshold + debounce + first-failure/recovery rules), `src/probeOutages/probeOutageService.js` (reconciles vs. stored events, run on probe-results ingest in `routes/agentReports.js`; an outage opening/escalating raises a `probe_outage.<metric>` finding through the finding sink, closing dispatches one recovery alert + a note on the event case — see `docs/probe-outages.md`); `probeOutagesRepository.js` + `probeThresholdsRepository.js`. See `docs/events.md` |
| Probe-outage reports / NIS2 draft | `src/routes/reports.js` (availability + probe outages + `/nis2-draft/:probe_outage_id`), `src/probeOutages/nis2.js` (English CFCS template); availability query in `probeResultsRepository.availability` |
| Event thresholds | `src/routes/thresholds.js` + `probeThresholdsRepository.js` (global default vs. per-location override); validation in `validation/probeOutageValidation.js` |
| Events platform (grouped anomalies, `event_cases`) | `src/eventCases/` — `eventCaseService.js` (auto-group findings per device within `EVENT_ACTIVITY_WINDOW_MS` — 15 min, `activityWindow.js`, the SAME window autoResolveJob calls 'finished' so there is no dead zone where an event is open but refuses new anomalies — + auto-correlate a config change ≤30m before; the auto-title names the agent + its site via `agentsRepo`), `deviceLabel.js` (pure `formatDeviceLabel` — "core-sw (Copenhagen HQ)", degrading to "device 1"), `stateMachine.js`, `autoResolveJob.js` (leader-only inactivity resolve), `timeline.js`, `similarity.js`, `askContext.js`/`askCache.js` (masked AI context) — + `repositories/eventCasesRepository.js`; router `src/routes/events.js` (mounted at `/api/events`, incl. `POST /:id/ask` reusing `analysis/assistant.js`). Overview widget in `src/dashboard/advancedDashboard.js`. UI `views.events`/`views.event` in `public/app.js` (tab label **Events**; the list's **Condition** column trims the stored title down to what its own columns do not already say, via `public/eventTitle.js`). Migrations 047/048/050. **Distinct** from probe outages above. See `docs/events.md` (vocabulary: event vs. incident vs. situation vs. probe outage) + `docs/event-cases.md` |
| Event recommendation (playbook → history → AI) | `GET /api/events/:id/recommendation` in `src/routes/events.js` — one combined answer in three ordered sections: (a) `matching_playbook` (looked up by the primary anomaly-type via `remediationPlaybooksRepository.matchByAnomalyType`; shows a prior run's result when the playbook already ran on this event), (b) `historical_matches` (reuses the Fase-4 `rankSimilar` helper, **resolved-only**), (c) `ai_suggestion` (Mistral fallback — only when a+b are empty or `?force_ai=true`, operator+; reuses the masked `gatherEventAskContext` + `assistant.suggestRemediation` + `askCache`, never fabricates a fix, `source:"ai_generated"`). Pure section assembly + the `shouldGenerateAi` ordering predicate in `src/eventCases/recommendation.js`; playbook schema/data-access in `src/repositories/remediationPlaybooksRepository.js` (migration 055) |
| Per-flow-pair volume baselines | `src/analysis/flowPairBaseline.js` (pure — `buildPairBaselines` groups by (dow,hour) + `median`/`mad` reuse, `zScore`/`classify`; no new stats) + `flowPairBaselineJob.js` (leader-only hourly rollup→recompute→score→emit findings, `metric flow.volume`, `hostId`=src); repo `src/repositories/flowPairBaselinesRepository.js` (`flow_pair_hourly` append-only + `flow_pair_baselines`); API additions in `src/routes/topology.js` (`GET /flow-baselines` operator+, `POST /flow-baselines/recompute`); migration 068; config `FLOW_BASELINE_*`. Fake `makeFlowPairBaselinesRepo`. See `docs/flow-pair-baselines.md` |
| Scheduled active discovery | `src/discovery/` — `cidr.js` (IPv4 CIDR math + `inScope` guard), `rateLimiter.js` (injectable-clock pacing), `probes.js` (native `net` TCP connect + `dns.promises` rDNS; ICMP unsupported default), `scanner.js` (`validateScope` refuses empty/invalid/over-cap; scoped probe loop), `discoverySweepJob.js` (leader-only sweep + hash-chained audit of scope/start/end/count). Admin-only router `src/routes/discovery.js` (`/api/discovery/*`, list/get/scan/promote/ignore — every path `requireRole(ADMIN)`); promotion creates an SNMP `agents` row via `agentsRepository.insertSnmpDevice`. Table `discovered_devices` (migration 069); config block `src/config.js` `discovery` (`DISCOVERY_*`). Fake `makeDiscoveredDevicesRepo`. See `docs/discovery.md` |
| New-device alarm (a MAC/IP never seen before) | `src/discovery/newDeviceDetector.js` — `createNewDeviceDetector` + `withArpDetection`/`withDiscoveryDetection`, which WRAP `arpEntriesRepo`/`discoveredDevicesRepo` in `src/server.js` (check before the upsert, raise after), so every writer is covered. Baseline guard (`NEW_DEVICE_BASELINE_HOURS`, 24) + hourly cap (`NEW_DEVICE_MAX_PER_HOUR`) → `device.new` findings (store/publish/event case/dispatch/ITSM); `NEW_DEVICE_ALERTS_ENABLED=false` switches it off. Vendor hint `src/identity/oui.js` (curated OUI subset). Shown on Changes as kind `new_device` (`changeFeed.fromFindings`). Long memory: `known_devices` (migration 131, `src/repositories/knownDevicesRepository.js`, 400-day retention) is consulted before a MAC is called new and touched after every ARP report; an ARP sighting of an IP discovery already reported inside the baseline window is not raised again. See `docs/discovery.md` |
| Topology change detection (LLDP neighbour changes) | **Pure diff** `src/topology/topologyDiff.js` (`diffSnapshots` prev vs reported → `neighbour_added`/`removed`/`link_state_changed`/`port_moved`; `isInverse`/`flapKey` for flap detection); **service** `topologyChangeService.js` (`processReport` — flap-collapse within `TOPOLOGY_FLAP_WINDOW_SECONDS` default 300 → single `flapping` record, persist, hash-chained `audit_log` write via the compliance logger, reconcile removed/moved edges); repo `src/repositories/topologyChangesRepository.js`; `lldpNeighborsRepository` gains `listByAgent`/`deleteEdge` + `link_state`. Diff seam in `src/routes/agentReports.js` (`POST /me/capabilities`, before upsert). **Surfaces:** new `topology` source in `src/timeline/targetTimeline.js` (rendered "Topology change" via `public/timelineView.js`), so changes appear in `GET /api/targets/:id/timeline`; dedicated `GET /api/topology/changes` (operator+, 400/404/500). Table `topology_changes` (migration 067). Fake `makeTopologyChangesRepo`. Tests `test/topologyDiff*.test.js` + `topologyChange*.test.js`. See `docs/topology-changes.md` |
| Consolidated Troubleshooting Dashboard | **Backend:** pure read-model `src/troubleshooting/overview.js` (`buildRootCauses` = the alarm rollup, preserving the cross-agent correlator's collapse rather than re-deriving it, with blast radius counted only BEYOND the affected set; `buildTopologyView` = derived node/link state — `agents.status` gives ok/down, tier-1 `directly_isolated` behind a down node gives `unreachable_downstream`, unknown status maps to `ok` so we never invent a fault; `buildAnomalies`/`buildSummary`) + fan-out `overviewService.js` (partial-failure per `targetTimelineService`; timeline reuses `buildTargetTimeline` with `agentId` re-attached by (source, ref_id)); router `src/routes/troubleshooting.js` (`GET /api/troubleshooting/overview` + `GET /api/troubleshooting/faults`, viewer+ filtered by role, 400/401/403/404/500/503). **Frontend:** `views.troubleshooting` + `tshootTopologySvg` in `public/app.js`, pure view-model `public/troubleshootingView.js` (`window.TroubleshootingView` — KPI cards, root-cause model, fault-row model + loading counter, change-vs-symptom split, brush helpers, deterministic layout), `.ts-*` CSS. The Active faults KPI carries a link that pages the raw alarms in on demand with an "x of y" counter — the page load never fetches them. "Show path" reuses `GET /api/topology/blast-radius/:node`; "What changed?" renders through `TimelineView.renderRow`. **No migrations.** See `docs/troubleshooting-dashboard.md` |
| Fault context hand-offs (agent/target/window between screens) | `openInContext()` / `contextActions()` / `applyContextFromUrl()` in `public/app.js`: Probes, Diagnose, Investigate, Device log and Troubleshooting read `?agent=&target=&window=` and open prefilled; events, situations, Changes, root causes, the agent page, Investigate results and search offer the hand-off row. `searchForNextView()` drops per-record chart params (`from`/`to`/`metric`/`overlay`) on a path change. See `docs/troubleshooting-dashboard.md` |
| Alert name + link | `src/analysis/alerting/alertContext.js` (dispatcher `enrich`: `hostName` + `link` from `BLUEEYE_PUBLIC_URL`, most specific record: situation → event → agent). See `docs/alerting.md` |
| Blast radius (impact from a failing node) | **Pure engine** `src/topology/blastRadius.js` (`computeBlastRadius` — tier 1: undirected `l2_link` BFS from the failing node → `directly_isolated[]`; tier 2: reverse `service_dep` BFS from failing+isolated → `dependency_affected[]`; each with a justifying path; depth-capped `BLAST_RADIUS_MAX_DEPTH` default 4, cycle-safe, `O(V+E)`); `blastRadiusService.js` builds `buildTopologyGraph` from the two bounded `listAll`s. **Surfaces:** `GET /api/events/:id` (viewer+) carries one added `blastRadius` field (computed on read from `host_id`, best-effort, no schema change); `GET /api/topology/blast-radius/:node` (operator+, `?depth=`, 404/400/500). Tests `test/blastRadius*.test.js`. See `docs/blast-radius.md` |
| Service dependency graph (`service_dep` edges) | **Backend:** `src/topology/serviceDependencyAggregator.js` (pure: TCP flow aggregates → host↔host edges, drop unknown/self, Top-N per source host), `hostResolver.js` (IP→host via `capabilities.ips`/SNMP host), `serviceDependencyJob.js` (leader-only rolling-24h recompute in `server.js` `backgroundJobs`), `graph.js` (`buildTopologyGraph` — unified typed `l2_link` + `service_dep` nodes/edges); repo `src/repositories/serviceDependenciesRepository.js` + `flowsRepository.tcpServiceFlows`; router additions in `src/routes/topology.js` (`GET /dependencies` viewer+ 404, `GET /graph`, `POST /dependencies/recompute` operator+); table `service_dependencies` (migration 066); config `SERVICE_DEP_*`. Fake `makeServiceDependenciesRepo`. Agent side (blueeye-agent 0.18.0): `netflow/aggregate.js` `flows` list + `localIps.js`. See `docs/service-dependencies.md` |
| Cross-agent event clusters | `src/analysis/crossAgentCorrelator.js` (pure detector — **target-aware**: each finding has a SUBJECT (`subjectOf`: probe target / switch / port / transaction test / the agent itself); findings within a sliding 5-min window of each other group on a shared subject across agents and sites, and different subjects only through a topology relation — same switch, upstream switch (blast radius), agent-level findings at one site, LLDP; the reasons are stored in `event_clusters.grouping_basis` (mig 130); the cluster is stamped on its members' event cases (mig 129)) + `src/analysis/crossAgentClusterService.js` (detect+dedup+resolve + opt-in **cluster-level AI advisory** for medium/high clusters via `assistant.suggestClusterCause`, stored in `event_clusters.advisory`, always published with its evidence list; best-effort) + `src/analysis/crossAgentClusterJob.js` (leader-only ~60s sweep in `server.js` `backgroundJobs`) + opt-in **cluster-level alerting** (medium/high) that fires ONCE per cluster via `dispatcher.dispatchCluster`, referencing (not resending) member alerts through the durable `alert_dispatch_log` (`repositories/alertDispatchLogRepository.js`, migration 059) + `repositories/eventClustersRepository.js`; tables `event_clusters` (migration 057 + `advisory` in 058). Cluster events reuse the `/ws/dashboard` broadcast (`{type:'event_cluster'}`). Fakes `makeEventClustersRepo`/`makeAlertDispatchLogRepo`. **Distinct** from the per-device `event_cases` and the per-target `correlator.js`. **Operator REST API + lifecycle:** router `src/routes/eventClusters.js` (`/api/event-clusters` — list/get/ack/resolve, RBAC + hash-chained audit), pure detail read-model `src/analysis/clusterView.js` (member hydration + `confidenceBreakdown` + network/application root-cause layer via the L2 `isAppMetric`/`isNetMetric`), `status` gains `acknowledged` + ack/resolve columns (migration 060); the auto-resolve sweep NEVER closes a cluster with an unacknowledged CRIT member and uses a 30-min default quiet period. **Automated read-only evidence on cluster open (Fase 6):** `src/evidence/` — `commandAllowlist.js` (`evidence-v1`, the single source of truth for the read-only items `iface.counters`/`arp.table`/`snmp.reads`/`agent.state`), `snapshotService.js` (captures over the existing `agentCommander.sendCommandAndWait` path — 30s/target timeout, concurrency 4, one 60s offline retry, Ed25519-signed with the release key, fire-and-forget from the sweep, NEVER blocks), `evidenceRetention.js` (6h age-out, skips unacked-CRIT clusters, default 90d) + `repositories/evidenceSnapshotsRepository.js` (gzip blob per (cluster, target), table `cluster_evidence_snapshots` mig 065); the agent enforces its OWN read-only allowlist (`blueeye-agent` `src/evidenceCollector.js`, defense in depth). API `GET/POST /api/event-clusters/:id/evidence(/:sid)`; timeline `evidence` source in `src/timeline/eventTimeline.js`. Fake `makeEvidenceSnapshotsRepo`. See `docs/cross-agent-correlation.md` |
| Baseline as context on a metric | **Shared component** `public/baselineMetric.js` (`window.BaselineMetric`, loaded via its own `<script>`): `compare`/`describe`/`ageTitle`/`render` + a PROVIDER interface (`{name, lookup(metric)->baseline\|null}`) so latency/loss/jitter register later without touching a call site. **The rule: no baseline → NO secondary field** (no placeholder — an empty space says "we don't know", a placeholder implies something was measured). Baseline 0 never divides by zero (`comparable:false` + absolute difference); a negative deviation renders as "60% below normal", not "-60% below"; <±5% reads as normal. Weekday names come from the i18n catalogue (`weekday.0`…`weekday.6`) — a slot is a day-of-week INDEX, not a date. **Backend** `src/routes/baselines.js` (viewer+). **SHIPPED REDUCED:** only flow-pair VOLUME is available — per-interface baselines are impossible (interfaces aren't persisted) and the latency/loss/jitter baselines in `src/analysis/baselines.js` are a disk FILE keyed by UTC hour with no day-of-week dimension. Tests `test/baselineMetric.test.js` + `test/baselinesApi.test.js`. See `docs/baseline-context.md` |
| Interface state transitions (up/down history) | **Pure diff** `src/health/interfaceStateDiff.js` (`diffInterfaceStates` prev snapshot vs freshly-computed `computeInterfaceHealth` output; `severityFor` — real link-down CRIT, virtual down INFO, recovery always INFO; `isReversal`/`isWorsening`); **service** `src/health/interfaceStateService.js` (ingest seam, last-traffic-sample-in-batch wins, flap collapse, best-effort so it can never cost the agent its results report); repo `src/repositories/interfaceStatesRepository.js`. Seam in `src/routes/agentReports.js` (`POST /results`). Surfaces via `fromInterfaceTransitions` in the changes feed (`kind:'interface_state'`). Table pair mig 075; retention in `analysis/retention/*`. Fake `makeInterfaceStatesRepo`. Tests `test/interfaceState.test.js`. See `docs/interface-transitions.md` |
| Changes landing page (what happened since I last looked) | **Backend:** pure read-model `src/changes/changeFeed.js` (per-source mappers, `withinWindow`, `correlateEvents` = `rollUpFindings` + `collapseRecurring`, `compareEvents`, `buildChangeFeed` — correlation runs BEFORE ordering and the cap so the cap's budget goes to distinct conditions, not repeats of one; grouping + cap applied to the ORDERED list so truncation drops the oldest, never a middle slice; `agentHealthRows` emits `currentState` rows for the two dimensions that are NOT transition-logged), fan-out `src/changes/changesService.js` (`Promise.allSettled` → `partial`+`failedSources`, per-source cap so one noisy source cannot crowd out the rest), router `src/routes/changes.js` (`GET /`, `POST /seen`; an unparseable `window` is a 400, not a silent default). Marker in `usersRepository.get/setLastSeenChanges` (monotonic). Fleet-wide repo additions `remediationPlaybooksRepository.listRunsBetween` + `configSnapshotsRepository.listBetween` (metadata only — never `config_text`). **Frontend:** `views.changes` + `changesRowEl()` in `public/app.js` (rows render through `TimelineView.renderRow` — shared shape, no second row renderer), `.chg-*` CSS, `PAGE_INFO.changes`; default route changed from `fleet` to `changes` and the nav gained a **Fleet** entry (the grid is still right for bulk ops). **Interface up/down is still missing** — interfaces are not persisted, so that needs a transitions table + an agent change rather than polling. Condition families `src/changes/indications.js` (metric → `latency`/`interface`/… ; unknown ⇒ `null`, never a guess) with the wording in `public/i18n.js` `changes.indicates.*` (en+da). Migration 074. Tests `test/changesFeed.test.js` + `test/changesApi.test.js` + `test/changesIndications.test.js`. See `docs/changes-feed.md` + `docs/events.md` |
| L2 path A→B, "where is device X", device inventory | **Pure** `src/topology/l2Path.js` (`buildSwitchGraph` — the graph.js resolution rule over `snmp_devices`+`snmp_neighbors`, CDP rows included via `protocol`; `pickAccessPort` — non-uplink port with the fewest MACs, `self`/`mgmt` = the switch itself; `computeSegment` — BFS hops, or two halves + a named `gap` with its evidence). **I/O** `src/topology/deviceLocator.js` (`createDeviceLocator` → `path`/`where`/`inventory`; identity from agents/ARP/router ARP/LLDP/discovery, bounded reads; `buildInventory` pure merge). Optional `deviceArpRepo` (router ARP, duck-typed `findByIp`/`findByMac`). Routes `src/routes/l2Path.js`; UI `public/views/pathLocation.js` (Diagnostics → Path & location, `/path-location`; jump from universal search via `openPathLocation`); CSS `.l2p-*` in `css/components.css`; docs/l2-path.md |
| Universal search (the entry point) | **Backend:** pure query analysis `src/search/query.js` (`classify` decides which resolver families a query could belong to, so a keystroke does not fan out to everything; `CONFIDENCE` is a fixed vocabulary not a score; `compareHits`/`dedupe`), fan-out `src/search/searchService.js` (`Promise.allSettled` → `partial`+`failedSources`, one dead source never blanks the field), router `src/routes/search.js` (viewer+, per-USER rate limit so one operator behind a shared proxy cannot lock out the NOC). CMDB reached via `makeCmdbSearch()` in `src/routes/cmdb.js` — the only place the search path touches credentials; its operator+ RBAC is deliberately NOT widened to the viewer+ field. Identity data from `arp_entries` (mig 073). **Federated sources:** events (`event_cases`/`event_clusters`), tickets (local `itsm_ticket_ref` + live ServiceNow `searchTickets`) and IPAM (Nautobot `searchIpam`, prefixes + addresses) — external thunks in `src/search/externalSources.js` (`makeTicketSearch`/`makeIpamSearch`, decrypt at call time, fan out over every enabled target); remote hits carry a `url` deep link. **Frontend:** `globalSearch()`/`searchHitEl()`/`searchTargetAction()` in `public/app.js` + `.search-*` CSS; the topbar pill and the `/` shortcut already existed. Tests `test/searchApi.test.js` + `test/searchQuery.test.js`. See `docs/universal-search.md` |
| Event work log / shift handover | migration `072_create_event_notes.sql`; repo `src/repositories/eventNotesRepository.js` (append + reads ONLY — no update/delete exists); validation `src/validation/eventNoteValidation.js` (`kind` is never defaulted); routes in `src/routes/events.js` (`GET`/`POST /:id/notes`, mounted only when the repo is wired so a pre-migration deployment degrades to 404 instead of 500); audit via `complianceLogger` (`event_note_append`). UI `eventNotesCard()`/`noteComposer()` in `public/app.js` + `.wl-*` CSS — ruled-out entries render pinned ABOVE the chronological log. Fake `makeEventNotesRepo`. Tests `test/eventNotes.test.js`. See `docs/event-work-log.md` |
| Event explanation (what/where/why) | `src/eventCases/explanation.js` (`buildExplanation`) — a light, separate section on `GET /api/events/:id` (NOT bundled into the recommendation): `what` = anomaly-type + severity, `where` = device/label/interface (+ Fase-6 topology, forward-compat), `why` = the finding evidence-array. `why` prefers a registered confidence model (`CONFIDENCE_MODELS`, empty today) and otherwise falls back to the RAW trigger-data (`observed`/`baseline`/`deviation` + evidence), `source:"raw_trigger"` — the documented fallback, not an error |
| Device config snapshots / diff / risk | `src/config/` — `diff.js` (uses the `diff` lib), `risk.js` (rule-based ACL/routing/interface=high), `mask.js` (IP + secret masking), `configContext.js` (masked+classified diff) — + `repositories/configSnapshotsRepository.js`; router `src/routes/deviceConfig.js` (`/api/devices/:id/config-history` + `POST /:id/config-snapshots`). Retention in `analysis/retention/` (`RETENTION_CONFIG_SNAPSHOT_DAYS`). UI = agent-page "Config history" card in `public/app.js`. Migration 049. See `docs/event-cases.md` |
| Probe findings + alerting | `src/analysis/probeFindings.js` (verdict→findings, reuses `health/probeHealth.js`) + `probePipeline.js` (runs on probe-results ingest in `routes/agentReports.js`) |
| AI assistant (explain + location summary) | `src/analysis/assistant.js` (OpenAI-compatible; opt-in; reads enable/provider/key/model/baseUrl live from the analysis config) + `src/routes/assistant.js`; provider presets (EU: Mistral/Scaleway/OVHcloud/IONOS/Aleph Alpha · US: OpenAI/Anthropic/Gemini/Groq/Together/OpenRouter/Azure · DeepSeek · Ollama self-hosted · custom — every entry region-tagged, admin's choice) = `src/analysis/assistantProviders.js` (`azure`/`custom` need an admin base URL). Runtime config (enable + provider + API key + model + custom endpoint): `settingsService.getAssistant/setAssistant` (`src/services/settings.js`), `PUT /api/settings/assistant`, UI `assistantSettingsCard` in its own **Settings → AI** tab (`settingsAiView`) |
| Fleet health (overview + verdicts) | `src/health/probeHealth.js` (`computeAgentHealth`/`mergeHealth`/`computeFleet`, median+MAD — folds in interface health), `src/routes/fleet.js`; UI `views.fleet`/`views.agent` |
| NIC firmware inventory / drift | `src/health/nicInventory.js` (`computeNicInventory`, groups by driver+PCI id, flags firmware outliers) from agent-reported `capabilities.nic`; HTTP `GET /api/fleet/nics` in `src/routes/fleet.js`; UI `views.nics` + per-agent NIC card in `views.agent`. Agent side in blueeye-agent `src/nicInfo.js` (`ethtool -i`) |
| Interface health | `src/health/interfaceHealth.js` (`computeInterfaceHealth`/`interfaceHealthSummary`); HTTP in `src/routes/interfaces.js` — agent side in blueeye-agent |
| Agent offline (runtime stale sweep, one finding per offline episode, dead-agent vs network-down verdict) | Pure verdict `src/health/agentOffline.js` (`assessAgentOffline`: site peers, other agents' probes to its IPs, switch port via ARP→FDB→`device_interfaces`/`link.down`, WS connection evidence); job `src/health/agentOfflineMonitor.js` (backgroundJobs in `server.js`; `agentsRepo.sweepStaleOffline`/`peerProbesTowards`; `agentWs.connectedAgentIds`). Metric `agent.offline`; verdict on the changes-feed row and the connection modal (`ag.offline.*`). Config `AGENT_STALE_OFFLINE_SEC`/`AGENT_OFFLINE_*`. See `docs/agent-connection.md` |
| Agent data-quality (drops/skew/version) | `src/health/dataQuality.js` (`computeDataQuality`); surfaced via `/api/fleet/health` + `/api/fleet/agent/:id` — all signals already sent by the agent |
| Per-agent traffic source (proc/snmp/netflow/sflow) | stored in `agents.monitor_config` (JSON); validated by `validateMonitorConfig` in `src/validation/agentValidation.js`; edited via `PUT /agents/:id` (`editAgent` modal in `public/app.js`); served to the agent by `GET /agents/me/config` (`src/routes/agentReports.js`). **Fleet-wide default** for newly enrolled agents lives in Settings → Agents (`agents` app_settings key: `defaultTrafficSource`/`defaultSflowHsflowd`, `settingsService.getDefaultMonitorConfig`), stamped onto the agent row at enroll (`services/enrollmentStore.js` via `routes/agentEnroll.js`); per-agent Edit always overrides it |
| A certificate expiry / an incident / an alert from Service Assurance | `src/serviceTests/assurance/` — `certificates.js` (the TLS handshake + what it reads), `policy.js` (what is worth reacting to, and how loudly), `reactor.js` (the sweep, the incident state machine, the notify port). Thresholds: the `assurance` section of `src/serviceTests/settings/defaults.js`. Alerts leave through the SAME dispatcher as analysis findings — wired in `src/server.js` (`assuranceNotify`) |
| Service Tests (anything) | `src/serviceTests/` — one module root behind `createServiceTestsModule(ports)`. Data model: `migrations/078_create_service_tests.sql`. Limits/budgets/caps: `src/serviceTests/settings/defaults.js` (defaults) overridden from `service_test_settings` (DB). Plan + security design: [docs/service-assurance.md](docs/service-assurance.md). V2 design of record: [docs/service-assurance-v2.md](docs/service-assurance-v2.md). Recording, for operators: [docs/service-assurance-recording.md](docs/service-assurance-recording.md). User journeys: [docs/service-assurance-journeys.md](docs/service-assurance-journeys.md). Self-healing: [docs/service-assurance-healing.md](docs/service-assurance-healing.md) |
| A Service Tests limit (discovery budget, allowlist cap, artefact retention) | `settings/defaults.js` holds the DEFAULT and its bounds; the effective value comes from the database via `settings/index.js`. Never add an env var for one |
| A Service Assurance step type | ONE entry in `src/serviceTests/engine/dsl.js` — validation, the designer's form controls, the runner's dispatch and the plain-language label are all derived from it. Then a case in `runner/execute.js` and a method on the driver |
| The Service Assurance UI | `public/serviceAssurance.js` (`window.ServiceAssurance`) + `serviceAssurance.css` (all `.sa-` prefixed); mounted by `views.serviceAssurance` in `public/app.js`, which passes the shared helpers in |
| The in-app **guides** (five next-next walkthroughs) | `public/guides.js` (`window.Guides`) + the `.guide-*` block in `serviceAssurance.css`; mounted by `views.guide` in `public/app.js` (nav group **Guides**, one `data-view="guide"` + `data-guide="<track>"` button per track: monitoring / fleet / diagnostics / assurance / insights — viewer+, the assurance one also `service_tests`), handed the shared helpers plus `openView`/`openTab`/`openSettings`/`openDocs`. Workflow **and** the values to enter, plus seven **action cards** that create the thing (site, enrollment code, ping, application, allowlist entry, first Service Assurance test, first mail-delivery monitor) through the same endpoints the screens use — role-gated, server errors shown on the field that caused them; Service Assurance's numbers are read from `GET /api/service-tests/settings`, the rest are pinned to `SCORE_WEIGHTS`, `THRESHOLDS` (`src/health/probeHealth.js`) and `ANALYSIS_DEFAULTS`/`RETENTION_DEFAULTS` (`src/services/settings.js`) by `test/guides.test.js`. Strings are `guide.*` in `public/i18n.js` (en+da). Whole-dashboard smoke test: `test/dashboardSmoke.test.js` (every nav page against the REAL routers over `makeApp()` — empty install, 500s, 401, populated + detail views, and the guided actions end to end). See `docs/guides.md` |
| A dashboard tab/view | `public/index.html` (button) + `views.<x>` in `public/app.js` + `PAGE_INFO` |
| The Logs menu (System Logs vs. User Logs) | Two admin-only nav entries in `public/index.html` → `views.logs` (the in-memory server stream, `GET /api/logs`) and `views.userLogs` (who did what, `GET /api/audit/users`). The User Logs read model + its flag rules are pure in `src/audit/userActivity.js` (`buildUserActivity`, `flagsFor`, `describeAction`); the router is `src/routes/auditEvents.js`. Names come from `users.name` (migration 093, display-only), set in Settings → Users. `PAGE_INFO.logs` / `PAGE_INFO.userLogs`; strings under `logs.*` in `public/i18n.js` (en+da). See `docs/audit-vs-logging.md` |
| Documentation / how-to content (Documentation tab) | `views.docs` + the `DOCS` content array + `docs*` helpers in `public/app.js` (static, no backend); reached from a **Documentation pill in the sidebar footer** (`.foot-docs`, `data-view="docs"`) beside the version line in `public/index.html` — not a nav-rail item; `PAGE_INFO.docs`; `.docs-*` + `.foot-docs` CSS. RBAC: the `Administration & setup` section carries `admin:true` and is dropped for non-admins. Prose guide in `docs/documentation-center.md` |
| Overview “open issues” rollup (events + findings) | feature `dashboard_advanced` (Professional+): `src/dashboard/advancedDashboard.js` (pure `buildAdvancedDashboard`) + `src/routes/dashboard.js` (`GET /api/dashboard/advanced`, gated by `requirePlanFeature`); UI `fleetIssues()` + `refreshIssues()` inside `views.fleet`, documented in `PAGE_INFO.fleet`. Merged into the Overview — no separate tab; below Professional the rollup is omitted |
| The look of a panel/card/table (radius, elevation, hairlines) | the token scale in `:root` + the "Soft surface pass" at the bottom of `public/styles.css`; rules of thumb in `docs/design.md`; guarded by `test/dashboardDesignTokens.test.js` |
| The dashboard language (en/da) | Catalogue `public/i18n.js` (`STRINGS.en`/`STRINGS.da`, `t('key')`, plus `plural('key', n)` → `key.one`/`key.other` with `{count}` filled in, swept by the UI gate); switched from the **account menu** in the topbar (`#lang-switch`, built by `renderLangSwitch()` in `public/app.js`, `.lang-switch*` CSS) or in `settingsAppearanceView` (Settings → Appearance). Persisted per user via `PUT /me/preferences` `{locale}` and mirrored into `localStorage` so the first paint is right. **The sidebar is static markup** that `render()` never rewrites, so it carries `data-i18n` / `data-i18n-title` / `data-i18n-aria-label` attributes and `applyStaticTranslations()` (called from `initLocale`, `setLocale` and `loadProfile`) walks them; the labels are `nav.*` and `guides.js` names the same tabs through the same keys, so a guide cannot tell you to click something the menu does not call that. The UI gate fails any `.tabs` control with no `data-i18n` |
| The NIS2 **report** language (en/da) | Separate, server-side catalogue `src/nis2/i18n.js` — the documents are rendered on the server, and `public/i18n.js` keeps one *active* locale in module state, which would let one request's language leak into another's document. `createT(locale)` returns a bound lookup, `t.enum(prefix, value)` translates an enum and passes an unknown one through unchanged. Locale per request: `?locale=` → `Accept-Language` → English (`localeOf` in `src/routes/nis2/_context.js`); the dashboard appends it via `withLocale()` in `public/app.js`. Executive + register documents translate their enums (they are read by management and by an authority); the Report Generator translates headings/labels only and hands back the register's own values. `dashboard.js` actions carry `code`+`params` next to the English `text` the API has always returned. Tests `test/nis2ReportI18n.test.js`. See `docs/nis2.md` |
| A dashboard colour palette (light+dark) | `PALETTES` + paired `[data-theme=…]` blocks in `public/styles.css`; picker `settingsAppearanceView` in `public/app.js`; per-user persistence via `/me` (`src/routes/me.js`, `usersRepository.get/updatePreferences`) + key whitelist in `src/validation/preferencesValidation.js` |
| License / feature gating | `src/license/*` (`features.js` = fail-closed gate + `requirePlanFeature` middleware; `plans.js` = plan/feature catalogue incl. `status` available/roadmap; `planService.js` = active-plan resolution + limits) + `src/services/usageService.js` (limit enforcement). Read-only API: `/license/plan`, `/license/usage`, `/license/matrix`. Feature **status & roadmap** tracked in **`ROADMAP.md`** + the Settings → License matrix Roadmap badge. See `docs/licensing.md`. |
| API tokens (programmatic access) | feature `api_access`: `src/routes/apiTokens.js` (admin CRUD, gated) + `src/repositories/apiTokensRepository.js` + `src/lib/apiToken.js` (mint/hash) + `src/auth/apiTokenAuth.js` (authenticates `X-API-Key`/`Bearer` → `req.user`, mounted in `routes/index.js`); table `api_tokens` (migration 034) |
| Audit log (who-did-what) | feature `audit_log`: `src/routes/auditLog.js` (admin read, gated) + `src/repositories/auditLogRepository.js` + `src/services/auditLogger.js` (fail-safe recorder); recording wired in `routes/auth.js` (login), `users.js` (user admin), `license.js` (re-validate), `apiTokens.js` + `reports.js`; table `audit_log` (migration 033) |
| Report exports (CSV / PDF) | `src/routes/reports.js` `*.csv` (gated `reports_csv`) + `*.html` (gated `reports_pdf`, print→PDF) via `src/lib/reportHtml.js`; NIS2 compliance pack gated `reports_compliance` in `src/routes/nis2/_context.js` |
| CMDB integration (single source of truth) | migrations `051_create_cmdb.sql` + `052_cmdb_custom_connector.sql` (`cmdb_config` singleton + `agent_cmdb_links`); repos `src/repositories/cmdbConfigRepository.js` + `agentCmdbLinksRepository.js`; validation `src/validation/cmdbValidation.js`; routes `src/routes/cmdb.js` — admin config + connection test (`/api/settings/cmdb`, `/test`, `/meta`), operator+ asset search (`/api/cmdb/assets/search` — one term matched against asset id / name / **location**; `/api/cmdb/assets/status` says whether a CMDB is connected at all), and per-agent links (`/api/agents/:id/cmdb-link`). Its **own** connector registry `src/cmdb/connectors.js` (ServiceNow / Nautobot / **`custom`** config-driven `src/integrations/connectors/customCmdb.js`, incl. optional static `headers`) — separate from the integrations registry — `testConnection()`/`search()` + `secretBox`; named presets (NetBox/i-doit/GLPI + built-ins) in `src/cmdb/presets.js`, served by `/meta`. Credentials encrypted at rest, never returned. Linking an asset **syncs `agents.location_id`** (match-or-create a location by the asset's CMDB location name). Also a target in the **Test area** in its own `cmdb` diagnostics group — NOT `itsm` (`screenCmdb` + `cmdb` in `src/routes/diagnostics.js`). **UI: Settings → CMDB** (`settingsCmdbView`, System-preset dropdown) + per-agent card (`loadAgentCmdbLink` — searchable dropdown `assetPicker` over id/name/location + removable chip). Docs: `docs/cmdb.md` |
| Outbound integrations (ITSM + CMDB/IPAM sync) | connectors in `src/integrations/connectors/*` (+ `index.js` registry with per-type `category` + `customItsm.js` config-driven "bring your own ITSM"); named presets (Jira/TOPdesk/GLPI + built-ins) in `src/integrations/presets.js`; trigger/debounce/retry/audit in `src/integrations/dispatcher.js`; HTTP in `src/routes/integrations.js`; validation in `src/validation/integrationValidation.js`; tables `integrations`/`integration_audit` (migrations 026/027). Events wired in `analysis/pipeline.js` + `probePipeline.js` (findings) and the enroll/agent-delete routes. **UI: Settings → ITSM** (`settingsIntegrationsView` in `public/app.js` — preset dropdown + CRUD + per-row test, credentials write-only). See docs/integrations.md |
| Test area (outbound screening) | `src/routes/diagnostics.js` (`/api/diagnostics/targets` + `/run`, admin) orchestrates each subsystem's own test primitive (alerting `dispatcher.test`, integrations `testFire`, `ldapAuth.testConnection`, `oidcAuth.testDiscovery`, reachability for SAML/assistant); pure posture lens in `src/diagnostics/screening.js`; reachability in `src/diagnostics/reach.js`; injected `diagnosticsFetch` (server.js → app.js → routes/index.js). UI `views.screening` + `PAGE_INFO.screening` + `.screen-*` CSS; each row deep-links to its setup via `settingsLink`. See docs/diagnostics.md |
| Local user creation (one-time password) | migration `056_user_temp_password.sql`; generator `src/auth/tempPassword.js`; email `src/services/userMailer.js` (reuses alerting SMTP, wired in `src/server.js`); routes `src/routes/users.js` (`POST /users/local`, `POST /users/:id/resend-temp-password`, `GET /users/local-availability`) + `src/routes/auth.js` (`POST /auth/change-password`, login temp-password/expiry handling); forced-change gate + wiring in `src/routes/index.js`; JWT flag `src/auth/jwt.js`+`src/auth/middleware.js`; repo `usersRepository` (`setTempPassword`/`clearTempPassword`); validation `userValidation.js`; UI `public/index.html` `#force-change` + `views.users`/force-change form in `public/app.js`. Gated OFF while SSO/LDAP active (403), and the gate **fails closed** — an unanswerable provider check refuses rather than allows. `POST /users` (admin-typed password) is deliberately **not** gated: it is the recovery path that keeps the fail-closed gate from locking admins out. See docs/user-local-otp.md + **docs/auth-lockout.md** |
| LDAP/AD authentication | `src/auth/ldap.js` (bind + group→role; **licence-gated** via injected `featureGate` → `sso_ldap`); login flow in `src/routes/auth.js`; config CRUD + login-audit in `src/routes/ldap.js`; validation in `src/validation/ldapValidation.js`; tables `ldap_config`/`ldap_role_map`/`ldap_login_audit` (migrations 028/029); gates: env `LDAP_AUTH_ENABLED` **+** licence `sso_ldap` (Professional, `src/license/plans.js`). **Dashboard UI = `settingsAuthView` (Settings → Authentication)** in `public/app.js`. Login fails **open** on any LDAP error (falls back to local auth) — that is what stops a directory outage locking admins out; see docs/auth-lockout.md. See docs/ldap-auth.md |
| SSO (OIDC) | `src/auth/oidc.js` (discovery + PKCE + id-token verify via JWK→KeyObject + `jsonwebtoken`; claim→role; **licence-gated** `sso_oidc`); browser flow `/auth/oidc/login`+`/callback` and admin role-map in `src/routes/oidc.js`; shared JIT provisioning `src/auth/provision.js`; validation `src/validation/oidcValidation.js`; tables `oidc_role_map`/`sso_login_audit` (migration 038); env vars `OIDC_*` (issuer/client id/secret/redirect). **Login-screen buttons** from `GET /auth/sso` in `public/app.js`. **Dashboard UI = `ssoSection(SSO_KINDS.oidc)` in Settings → Authentication** (status, test, claim→role map, sign-in audit). See docs/sso-oidc.md |
| SSO (SAML) | `src/auth/saml.js` (SP-initiated; assertion verify via hand-rolled exc-c14n + XML-DSig in `src/auth/samlXml.js` — no XML lib; signature+digest+issuer+conditions+audience+expiry; attribute→role; **licence-gated** `sso_saml`); SP flow `/auth/saml/login`+`/callback`(ACS)+`/metadata` and admin role-map in `src/routes/saml.js`; validation `src/validation/samlValidation.js`; table `saml_role_map` (migration 039) + shared `sso_login_audit`; env vars `SAML_*`. Test signer: `test-support/samlTestkit.js`. **Dashboard UI = `ssoSection(SSO_KINDS.saml)` in Settings → Authentication**. See docs/sso-saml.md |
| Encrypting a secret at rest | `src/lib/secretBox.js` (AES-256-GCM, keyed by `SECRET_ENCRYPTION_KEY`→`JWT_SECRET`); store the token in a `*_encrypted` column, decrypt only at use |
| Audit trail (Reporting → Audit) | unified server-wide audit, **admin only**: middleware `src/middleware/auditLogger.js` (records user actions in `res.on('finish')`, mounted in `src/app.js`) + pure helpers `src/audit/actions.js` (`describeRequest`/`redactBody`); agent activity recorded on ingest in `src/routes/agentReports.js`, agent-reported errors (`agent.error`) plus connect/disconnect (`agent.online`/`agent.offline`) over the WS channel in `src/ws/agentSocket.js`, and enrolment (`agent.enrolled`) in `src/routes/agentEnroll.js`; repository `src/repositories/auditEventsRepository.js` (`record` discrete / `recordRecurring` repeat-suppressed via UNIQUE `dedup_key`); read router `src/routes/auditEvents.js` (`/api/audit`); table `audit_events` (migration 035); UI `auditModule()` in `public/app.js` (Reporting → Audit). See docs/audit.md |
| Reporting (NIS2 + Report Generator) | `src/routes/nis2/` (one file per resource, mounted in `routes/index.js`); pure scoring in `src/nis2/dashboard.js` (`computeDashboard`/`recommendedActions`), report build + print-ready HTML in `src/nis2/report.js`, the selector-driven custom-report engine in `src/nis2/reportBuilder.js` (`SOURCES`/`sourcesFor`/`buildCustomReport`/`customReportToCsv`), shared enums in `src/nis2/constants.js`; repositories `src/repositories/nis2{Risks,Controls,Events,Reports,Evidence,Audit}Repository.js`; validation `src/validation/nis2Validation.js`; tables in migration 031; UI `views.reporting` (sub-sections **Findings** `findingsReport()` — first, and the only one that needs no setup — + **NIS2** `nis2Module()` + **Report Generator** `reportGenerator()`), get-started guide `nis2GetStarted()`, + `PAGE_INFO.reporting` in `public/app.js`. See docs/nis2.md |
| The executive network report ("fix these issues, in these places") | `src/analysis/networkReport.js` — PURE: summary + trend in, a ranked document out. `buildNetworkReport` ranks the places by CRIT then WARN, names at most `MAX_ISSUES_PER_PLACE` metrics each, and reads the direction off the first half vs the second half of the trend (`unknown` under four buckets). `conclusion` is the one sentence at the top. `renderNetworkReportHtml` reuses the NIS2 print chrome (`src/nis2/report.js`) and the server-side catalogue `src/nis2/i18n.js` (`net.*`, en+da). No model is ever called — `test/findingsReport.test.js` pins that. Served by `GET /api/findings/report`; UI `findingsReport()` under Reporting in `public/app.js` (charts the same numbers: severity over time, then by metric and by place). See `docs/analysis.md` |

## Conventions

- **CommonJS only** — `require`/`module.exports`. Not TypeScript, not ESM. No build step.
- **No US-based vendors/SDKs** — tiles, GeoIP, geocoder, fonts are EU or self-hosted.
- **Privacy by design** — metadata only (5-tuple, ports, ASN, timings); never payload/DPI.
  RFC1918/private addresses are never geolocated.
- **Analysis is local + explainable** — median+MAD robust z-score, no ML libraries, no cloud.
- **Every finding/result carries an explanation + evidence.**
- **Tests:** `node --test` (auto-discovers `test/**` + `src/**/__tests__`). Express
  endpoints are tested for 400/401/403/404/500. Outbound calls (LLM/SMTP/geocoder) are
  mocked. Repos are tested with a fake `pool`; routes with `makeApp` + fakes.
