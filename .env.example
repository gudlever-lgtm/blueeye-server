PORT=3000
WS_PORT=4000
DB_PATH=/data/blueeye.db
RCA_URL=http://blueeye-rca:5000
RCA_ENABLED=true
# Comma-separated apiKey:role pairs gating the /locations endpoints.
# Roles: viewer < operator < admin. Example:
API_KEYS=viewer-key:viewer,operator-key:operator,admin-key:admin
